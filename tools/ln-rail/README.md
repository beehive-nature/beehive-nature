# THE LN RAIL MODULE — receipts, state, and the one parked leg
**Order:** founder, 2026-08-29 ("fire all guns"). **Dossier:** docs/dispatches/CHIEF_ORDER_LN_RAIL_2026-08-29.md. **Formulary:** RAIL-FORMULARY-1 §LIGHTNING (ratified @db12e7d).

## What stands LIVE (receipts)
- **The node:** Alby Hub v1.24.0 (ghcr.io/getalby/hub, Apache-2.0) on the oracle box, docker `bnr-albyhub`, loopback-bound `127.0.0.1:8081` only (fence: module, not a public paid lane). Embedded-LDK backend, mainnet, synced. **Node pubkey: `03b717b54f326fa562e9e58dd672c229e655b56a87b73f0130ae66dbba407529ae`** PUBLIC-CONSTANT (a lightning node pubkey is public by nature) (GET /api/node/connection-info). Admin secret: `~/bnr-albyhub-admin.pass` on the box (0600, ssh-user home — passwordless sudo covers docker only, root-file writes were the first setup trap).
- **The genesis-receipt invoice:** `lnbc2100n1p4fym6hd…qwneuql` — 210 sats, mainnet, pending, paymentHash `093da899939de4ab4220f8dec06cab2e2430efbccd632db557f6032a1fe702b0` PUBLIC-CONSTANT (a payment hash is public by nature). **Payment deferred and WHY: the node is unfunded (zero channels) — money dormant per the coherence fence.** Invoice creation needed JIT-channels disabled first (PATCH /api/settings `{"jitChannelsEnabled":false}` → 204) because the LSP refuses inbound liquidity to an unfunded node — the fence asserting itself through the LSP, correctly.
- **The allowance connection:** app `z1-agent` on the hub — scopes `pay_invoice/get_balance/list_transactions`, **maxAmountSat 1000, budgetRenewal daily** — the LN-side genesis allowance shape ($1/day analogue), revocable by deletion/rotation at the hub. Budget enforcement is hub-source code we cited in the dossier: `frontend/src/components/BudgetAmountSelect.tsx` + `BudgetRenewalSelect.tsx`, `db/queries/get_budget_usage.go`, `nip47/controllers/get_budget_controller.go` (repo tree, master @2026-08-29). **API shape receipt:** POST /api/apps takes `{name, scopes[], maxAmountSat, budgetRenewal:"daily", isolated}` and returns the pairing URI + secret; POST /api/start takes `{unlockPassword}` and returns the JWT (body token, not cookie — the session/unlock field traps cost three probes; `password` ≠ `unlockPassword`).
- **The bridge (ours):** `nwc.mjs` (minimal NIP-47 client — one-shot fresh app key per call = rotation-revocation for free; NIP-44 v2 after getalby's relays rejected NIP-04), `mcp-server.mjs` (MCP stdio, tools = pay_invoice + get_balance — an agent's money verbs countable on one hand), `prove-quota.mjs` (the proof harness). getalby/nwc-mcp-server = pattern-only, unlicensed, never a dependency (dossier §4).
- **The swap seam:** `swap-adapter.mjs` — pluggable interface, **REGISTERED_ADAPTERS = {} BY LAW** (no live default), Boltz contraindicated on the label (dead 2026-08-03, refund-only API), Blockstream Swaps noted as beta candidate, the hub's OWN built-in swaps are Boltz-based (routes /api/swaps/in|out) — same label applies.

## The parked leg (state, precisely)
**"Agent pays a capped invoice; over-cap returns QUOTA_EXCEEDED" — proven at code-cite level, blocked at the relay ACL live-run.** The bridge derives the app keypair correctly (derived pubkey byte-matched the hub's `pairingPublicKey: dd5d5dfe…`), encrypts NIP-44, and reaches the relays — but getalby's NWC relays refuse our kind-4 publishes ("kind 4 not allowed" on relay2, "blocked: Event rejected" on relay1 — NIP-42 auth is the top hypothesis; SimplePool does not answer AUTH challenges), and our own buzz relays are invite-gated for writes (G-lane join law). Attempted repoint via PATCH /api/settings `Relay` (204 but did not persist; `relays:[]` after restart — key name or restart-order issue unresolved).
**Two unparked paths, both small:** (a) add NIP-42 challenge-response to `nwc.mjs` (a AUTH/sign dance, ~20 lines) and run the proof on getalby's relays as-is; (b) give the z1-agent connection a write grant on `skaists.buzz` (relay-side policy — our relay, our law) and repoint the hub's relay config properly (env var `RELAY` per config/config.go:50-51 is the documented override — `RELAY=wss://skaists.buzz` in the docker env beats the settings API).
**What is already proven regardless:** the hub enforces the budget BEFORE payment routing (nip47 controller order), so over-cap → QUOTA_EXCEEDED is the wallet's documented, code-visible behavior; the missing inch is live-wire demonstration through a relay that accepts our publish.

## Fence check
Identity/wallet/allowance provisioning only. No paid lane opened, no channel, no swap wired, money dormant. The founder's flip (fund the node, wire a swap adapter, open the paid till) is the only thing that changes this.
