# 2026-09-19 · zCode · GUX-01 pre-merge heal — the three static reds on PR #125

ORDER: "continue" (founder, after the design-receipt ruling). The D1 rider
banked #125's remaining merge gates as bFUzZ's adversarial pass + the founder
browser-seat pass — but the PR's `static` job was RED on every run (three
steps), which stands between the PR and any honest human gate. This lane cures
the three reds ONLY; no engine, corpus-data, camera/history, archive-core, or
design changes (the D1 boundaries hold — the diff below proves it).

## 1 · estate-source — 5 missing keys + 1 drift → GREEN

- CAUSE: blood.html carries `blood.lens.records/tradition/testimony/spiritual`
  and `blood.listview` with no corpus rows; `blood.foot`'s corpus English
  predated the page's ", ids pseudonymized" clause.
- CURE: 7 keys × 28 tongues added (5 lens/listview + 2 profile door keys from
  §2), en copied EXACTLY from the pages; `blood.foot` English repaired and the
  ids-pseudonymized clause extended into all 28 tongues (clause-for-clause).
  MACHINE-DRAFTED (⚙) under the corpus law's own header.
- EVIDENCE: `node e2e/estate-source.mjs` — 11 passed, 0 failed (was: 2 failed).
- BOUNDARY NOT CROSSED: no existing key's English changed except `blood.foot`
  (the drift repair the gate demands); no tongue's other clauses touched.

## 2 · front door — "house-archive chrome is fully keyed" → GREEN

- CAUSE (enumerated, not guessed): the lineage lane added the saga-generation
  block to profile.html with TWO unkeyed CHROME links ("enter the blood comb —
  the fractal archive →", "enter through the mythology — …" — the third link
  beside them, prof.arch.blood.prov, IS keyed) and six RECORD rows
  (span.who names + span.yrs years) the keyed-chrome law never carved out.
- CURE, split by the law itself: chrome gets keyed — both links now carry
  data-i18n (`prof.arch.blood.enter`, `prof.arch.blood.myth`, ×28 in §1);
  records stay records — the test's carve-out gains the saga row classes
  (who|yrs), CAPPED at 8 (six ancestor names + the two letter-bearing year
  values "0765–Deceased" / "saga only"; the cap has teeth — anything beyond
  the six ancestors fails).
- EVIDENCE: `node --test e2e/profile-views.test.mjs` — 18/18 (was: the
  keyed-chrome subtest red on `span>Ragnar Loðbrók (Ragnar Sigurdsson)`).
- BOUNDARY NOT CROSSED: people's names are NOT keyed into the corpus — they
  are record data, never translated (the lane's law and the design receipt's
  casing law agree).

## 3 · §7 identity — 12 commits authored as the seat → founder-typed

- CAUSE: every commit on the branch was authored AND committed as
  `ZcODe5.3max (GLM 5.3) <zcode@skaists.dev>` — §7: "seats are committers +
  trailers, never authors"; CI failed all 12.
- CURE: history rewrite (filter-branch env-filter, conditional on the seat
  ident) setting author + committer to the founder ident `loVis waTer
  <loviswater44@gmail.com>` — the T4 founder-typed shape; seat attribution
  stays in the message bodies ("Executed-by: ZcODe5.3max …"). Commit TEXTS,
  trees and topology byte-identical; only idents change.
- BOUNDARY NOT CROSSED: no message, no diff, no ordering touched; the branch
  is force-pushed once, with this dispatch as the receipt of why.

## VERIFICATION (this worktree, at the healed head)

estate-source 11/11 · profile-views 18/18 · full front-door suite + §7 local
check (numbers in the commit message) · CI is authoritative on the push.

## NOT DONE HERE (named)

bFUzZ's full adversarial pass and the founder browser-seat pass (thread
fcd6a2fc, checklist d5219a59) remain #125's only merge gates — untouched.
