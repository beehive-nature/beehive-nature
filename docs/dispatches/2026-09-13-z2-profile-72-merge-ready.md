# z2.profile — MERGE-READY RECEIPT · PR #72 on current main · 2026-09-13

**Status:** #70 is **MERGED** (main @9bdb946c); #72 is **rebased onto current main** (base retargeted from the old stack branch to `main`), still **DRAFT** — the final integration call belongs to the founder, per order.

## the path this took (why the receipt is late)

Main moved twice under #70 (translation tranche #69, gallery #71/#73, and the house-profile rework of `surfaces/profile.html`). GitHub marked #70 CONFLICTING; the merge order required resolving it. Resolution shape: **main's rewrite taken whole**, this lane's pieces re-applied on top — with one real find: the rails band first landed inside the beat-gated `#instrument` wrapper (default-hidden until "Go deeper"), which broke bee/raver register prose until the band moved outside it. Two CI laws banked in the landing, both now in the tree's comments and this lane's memory: the **no-dead-host sweep scans literals** (a comment *naming* the dead eosn endpoint fails it — comments now name nothing), and **§7 computes its push range from the push event's before-SHA** (a force-push abandons it and the check fails closed — cure: reshape history, then always land an incremental rider commit). #70 merged clean after that: all four checks green (node 8m34s · scan · static · test).

## the rebase

#72's single entry-point commit replayed onto main @9bdb946c. Text auto-merged correctly (verified: one band per surface, five-rail intros, vending deep-link ×1, ERC20i + bnr:// cards in place, zero markers); the only conflicts were binary screenshots, resolved to this lane's side and then **regenerated by the re-run below**.

## gates on the rebased #72 (all local, this seat, this session)

- **`node e2e/profile-caps.mjs` → 54 passed, 0 failed** — the full ordered battery: register swaps, checker verdicts (HELD with owner/account/expiry, honest not-found, `.a` same-row), book resolution incl. `bclaude.a` live-truth, storage purity, six 390px in-register renders, vending copy truths (not-available wording present, old one-tap line gone, both money rails held at the label, not-a-PayPal-checkout), five separate cards per surface with exactly one coming-soon card, boundary + href assertions, SP never-claims, zero page errors on all three surfaces.
- **`node scripts/estate-check.mjs`** → PASS — 94 counted · 103 listed · 3 orgs · counts computed, not written · hub in sync.
- **`node e2e/estate-source.mjs`** → 11 passed, 0 failed (corpus integrity).
- **`node e2e/university-smoke.mjs`** → **87 passed, 0 failed**.
- **`node e2e/no-dead-host.test.mjs`** → 11 passed, 0 failed.
- **390px review** → no horizontal overflow and zero page errors at **390 and 1440** on dynasty, holder, and vending.

## what #72 changes relative to main (the whole delta)

Five separate capability cards on both profile surfaces (.b · .a with the vending deep-link + verbatim rehearsal/testnet boundary · Silent Payments display-only · ERC20i with JEDI law · bnr:// with prefilled resolver link), five-rail intros in three registers, and the vending payment-copy corrections (card/PayPal NOT AVAILABLE, USDC/PYUSD HELD until the founder names the seat, PYUSD is not a PayPal checkout). No behavior changes to wallets, keys, or chain reads beyond what #70 already merged.

**Awaiting the founder's final integration call. No merge, no deploy by this seat.**
