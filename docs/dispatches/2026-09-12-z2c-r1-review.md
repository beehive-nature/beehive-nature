# z2.c R1 re-review — 62c60816

Candidate remains unaccepted. Independent baseline: 114/114 offline under `cargo test -p watchpay --locked --offline --quiet` in detached wt-astra-z2c-review. GitHub test/node/static/scan all success at 62c60816. Prior wire/summary/freshness corrections and same-plan replacement regression are present. No builder worktree edits or live actions.

## P1: verified identity discarded at final recording boundary

`connect.rs::record_verified_signed` passes the verified attempt number and decoded transaction to the ledger, but never compares `request_plan_hash` or `request_operation` against the supplied plan/destination. Companion `2026-09-12-z2c-r1-probe.rs` appended to adapter_signing.rs reproduces: compose and verify under plan A; create otherwise identical plan B with another job_id and recomputed plan_hash; open B intent with matching nonce and attempt sequence; record A's verified result under B. It succeeds. Probe result 0 passed / 1 failed; 47 existing adapter tests filtered out.

Command: `cargo test -p watchpay --locked --offline --test adapter_signing astra_r1 -- --nocapture`.

Correction: enforce exact verified plan identity and operation/batch identity at the final adapter boundary before any write, as well as the attempt binding already added. Preserve destination ledger bytes/state/reservation on refusal. Include matching-plan control and wrong-operation rejection. Do not rely on equivalent calldata to establish application/job authorization.

## P1: claimed atomic transition remains an unlocked read/check/write

Source finding, not a scheduled race reproduction: Ledger stores only root: PathBuf; open() allows multiple handles/processes; record_signed_inner reads attempts, checks sequence/state, then calls persist/atomic_write without any mutual exclusion. Concurrent cancellation or replacement can occur between read and write. Atomic file rename protects replacement of one file, not the checked state transition or multi-file budget decisions. The claimed guarantee and the previous review's atomicity requirement remain unmet.

Use an enforceable exclusive-writer contract for this ledger or a maintained locking mechanism covering the entire conflicting read/check/write operation. Every competing mutation must participate. A mutex in one instance alone does not exclude a second handle/process. Add deterministic contention tests across independent handles/processes (not probabilistic sleep races), including cancellation versus signing, and prove only a lawful transition wins with reservations consistent. State precisely what OS/process/crash scope is proven; do not claim power-loss guarantees.

## Correction order

Same z2.c session, GLM 5.3 MAX. Reproduce the identity probe, implement the two bounded corrections, preserve the 114-test semantics, run fmt/offline tests/clippy/scan, and push a canonically attributed descendant. Return exact SHA and hosted checks for independent review. No device preflight, live SDK, wallet, RPC, real signing, broadcast, upload, deployment or merge. z2.d web release stays separate.
