# bViEw three-temperature progressive disclosure (2026-09-21)

**Seat:** Cowork / loVis. **Surface:** `surfaces/bview.html`.
**Gold:** Autonomi local "Your file" screenshot (`docs/dispatches/autonomi-your-file-nerd-ref.png`) — cream instrument sheet, labeled fields, honest finish line. Founder charter: one set of facts; register changes prose/density, never capabilities.

## What the watcher feels

| Register | Feel |
|---|---|
| **New bee** (default) | Calm paste → Open → video. Short chrome. No step lists, no jargon wall. Progress is `MB · MB/s` only while bytes move. The download sheet stays one tap away (`data-reg-disclose`). |
| **Raver** | Same door, softer first paint — pulse language on the wait row, empathic lead ("feel the pulse as the picture arrives"). Imagery/pulse tone; no new mechanics. |
| **Cypherpunk** | Autonomi-class instrument sheet on phone (~390). Cream "Your file" card. Disclose opens by default. |

## Nerd fields — real vs silent

**Real (cite-or-silent — only when this client or the estate door returned them):**

- Address
- Downloaded size + rate (MB/s)
- Content-Length
- `X-Ant-First-Chunk` HIT/MISS when the header is present (edge first-chunk cache, #208)
- Accept-Ranges when present
- TTFB (fetch start → response headers)
- Time-to-first-frame when a frame painted
- Path: `stream` | `envelope` | `cache`
- SHA-256 of received bytes when SubtleCrypto finishes the Blob

**Silent / labeled "not measured" (Autonomi demo network stats we do not have):**

- Chunks, chunk requests answered, lookup answers, lookup traffic

Never invent peer maps or "no server involved" — this surface uses the estate ANT door (`relay.skaists.dev/ant/v1`). Finish line: `Done in Xs via the estate door`.

## Playback preserved (#203 / #205 / #206)

Progressive binary `/stream`, stub abort &lt;500 ms, PLAY_MIN 12 MiB, MB/s counter, playhead hold while early mdat grows — unchanged happy path. Shared shell via `tour.js` (#207) still mounts register + lang.

## Gates

`node --test e2e/bview.test.mjs` · `node e2e/estate-source.mjs` · register shared-shell unchanged.

## CORS expose (#208 door + e2e mock)
Cypherpunk sheet `n-chunk` needs `Access-Control-Expose-Headers: X-Ant-First-Chunk, Accept-Ranges` on the live door (and in stream mocks). Without it the browser hides the header and the sheet stays on em-dash.
