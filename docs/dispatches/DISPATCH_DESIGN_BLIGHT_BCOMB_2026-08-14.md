# DISPATCH → Claude Design: the bLighTnetWorK visual language
**From:** Seat 3, relaying founder orders 2026-08-14 · specs/canvases only, no code

## The vocabulary (exact casings — house law, never normalize)
- **bLighTnetWorK** — the optical air-gap network (screen→camera ceremony transport)
- **bLiGhTbeAM** — one directed transmission; ALSO the plugin-install rail (founder:
  onboarding bDiD is the user's first beam, so plugin installs reuse a gesture they
  already know). Rate brand: **~420 b/sec**.
- **bComb** — the frame format: b/w hexagonal data comb (luminance-only bits) + colored
  mandala halo (zero bits). **Inverted white-on-black is the native dark polarity**
  (founder's black-background mandala = the reference; "the b logo moving and
  transferring data" is the brief in one sentence).

## Orders
1. **bComb visual spec** — from the founder's mandala assets: data-comb geometry vs halo
   ring split, both polarities, and the STATIC frame look. tokens.css is law (D-1:
   color never carries meaning alone; honey chip law untouched).
2. **bLiGhTbeAM motion spec** — how the comb animates while streaming (the "waggle"):
   cycle cadence, what stays fixed (finder/halo) vs what dances (data cells), a
   reduced-motion variant. Target surfaces: wizard pairing screen, receiver, and
   someday the Trezor bSAFE 7 screen (b logo dancing on the device).
3. **Surface copy sweep mock** — receiver header becomes "Receive on the
   bLighTnetWorK"; wizard optical card copy carries the beam vocabulary.
4. **Safe 7 homescreen art spec** — founder's b pixel art: verified constraints so far:
   **64 KB max (MODEL_HOMESCREEN_MAXSIZE 65536), hardware JPEG decoder** (JPEG native).
   Exact RESX×RESY: goose's THP matrix dispatch will pin it; design at vector so it
   rasterizes to any grid.
5. **Corrections list** (not code) auditing the live surfaces
   (`surfaces/onboarding/{index,receive}.html`) against DESIGN-BRIEF-01/03.

Cowork: INDEX rows for this + the goose dispatch + the naming receipt (standing duty).
