# 2026-09-07 — cloud-env: a Cursor Cloud Agent development environment

Lane: cloud-env. Seat: Cursor Agent (committer), founder-authored per §7.

## What landed

A repository-managed Cloud Agent environment so a fresh agent boots straight
into the same green CI runs on every push, and the static estate is served and
browsable without any hand-setup.

- `.cursor/environment.json` — repo-managed config (highest-precedence source).
  - `install`: `bash .cursor/install.sh`
  - `terminals`: `surfaces-hub` → `python3 -m http.server 8080 --bind 0.0.0.0`
  - `ports`: 8080 (the front door / surfaces hub)
- `.cursor/install.sh` — idempotent bootstrap that MIRRORS
  `.github/workflows/tests.yml`, so "it built here" and "CI is green" are the
  same fact:
  - `git config core.hooksPath .githooks` (the secret-scan + §7 hooks)
  - `cargo build --workspace --locked` (34-crate kernel)
  - `cargo build --locked --manifest-path rust/bsafe-host/Cargo.toml`
  - `npm ci` in `e2e/`, `key-build/bdid-key/`, `ops/voice-scribe/`
  - `npx playwright install --with-deps chromium` for the browser suites

No dev servers live in `install` (it must terminate); the hub server is a
`terminals` entry so its log and lifecycle stay visible.

## Receipts (all run on the provisioning VM — Rust 1.98.1, Node 22, Python 3.12)

Rust workspace:

- `cargo build --workspace --locked` → exit 0
- `cargo test --workspace --locked` → **1003 passed; 0 failed; 3 ignored**
- `cargo fmt --all --check` → exit 0
- `sh scripts/lint-shell-chains.sh` → exit 0
- `cargo test --locked --manifest-path rust/bsafe-host/Cargo.toml` → all ok
- `cargo run --locked -p bindexer -- prove-keyless` → exit 0 (SPEC-BINDEXER-0)

Static / node gates:

- `node scripts/estate-check.mjs` → exit 0
- `node --test e2e/atlas.test.mjs e2e/agent-dock.test.mjs e2e/lang-coverage.test.mjs`
  → 26 pass / 0 fail
- `node e2e/estate-source.mjs`, `node e2e/dock-claims.mjs`,
  `node e2e/forge-freeze.mjs` → all exit 0

Browser suites (Chromium via Playwright):

- `key-build/bdid-key`: `node test/test.mjs` → 26 pass / 0 fail
- `e2e/e2e.mjs` (onboarding, CTAP2 virtual authenticator + PRF) → 6 pass / 0 fail
- `e2e/university-smoke.mjs` → 78 pass / 0 fail

Hub served & exercised end-to-end:

- `python3 -m http.server 8080` → `/` (200, redirect) and
  `/surfaces/index.html` (200)
- Search filter on the hub, driven deterministically: **101 → 22** for `art`,
  **→ 0** for `escrow` (no surface matches that kernel term), **→ 101** on
  clear; 0 console errors. Also verified live in a recorded browser walkthrough.

Install idempotence:

- `.cursor/install.sh` run twice back-to-back → both exit 0; `git status`
  shows **no `Cargo.lock` / `package-lock.json` drift**.

## Validation via a prebuilt environment build

- A disk snapshot of the working VM was taken and reached READY.
- A draft build was triggered from that snapshot + the branch's install and
  **SUCCEEDED**; its log shows `install.sh: complete` with zero errors.
- A fresh cloud agent booted from that build confirmed: rustc/cargo 1.98.1,
  node v22.14, python 3.12.3; the baked `install` artifacts (Rust `target/`,
  the three `node_modules` trees, the Playwright chromium cache); the
  `chain-zano` derivation proof (`9 passed; 0 failed; 1 ignored`); and
  `estate-check` green.

### Honest caveat — `terminals` auto-start could not be observed here

The `surfaces-hub` server is declared as a `terminals` entry (the mechanism the
env-setup skill prescribes for a visible dev server). It serves correctly when
running (200 on `/` and `/surfaces/index.html`, verified live), but its
per-boot AUTO-start could not be observed through the tools available to this
run: cloud SUBAGENTS skip the environment bootstrap entirely (a just-in-time
subagent boot showed no `/tmp/cursor`, no install run, no tmux session — install
artifacts only appear when baked into a build's snapshot). Auto-start of
`terminals` is a top-level-agent behavior; it is not exercised by subagents.
The config is schema-valid and is the standard repo-managed mechanism. For the
dashboard-Save (db-managed) path — whose schema carries only `install`/`start`,
not `terminals` — the proposal additionally includes an idempotent `start` that
brings the hub up, so both the repo-managed and db-managed paths serve the hub.

## Notes

- `protoc` is NOT required: there are zero `build.rs` files in the tree, so the
  frozen proto (`proto/messages-zano.proto`, v0.3) is not codegen'd at build.
- The 3 ignored Rust tests are the firmware-gated slip0010 track and name their
  own blocker (see README / TREE-CENSUS), not an environment gap.
- `playwright install --with-deps` uses passwordless root on the provisioning
  VM to satisfy apt browser deps; the second install run confirmed the apt deps
  were already present ("0 upgraded, 0 newly installed").
