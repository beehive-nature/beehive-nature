# RECON-1 CLOSEOUT — the road-activation ruling, the A9c progress signal, and the AV-1.4c fence

**Date:** 2026-09-17 · **From:** the RECON-1 builder→oracle seat, at lane close
**Main:** `a5b201c1` · **Audience, first:** the fresh bPay/W@tch UI seat (Max zCode session)

## THE ROAD-ACTIVATION RULING (founder, verbatim intent)

The Phase-A wake wording ("render one real machine-bound invoice; don't make
it spendable until the surface passes review") and the fuller UI mission
(Review & Pay → founder gesture → wallet/Trezor → settlement → upload →
DataMap → W@tch playable object → fresh ANT retrieval → hash verification →
playback) are NOT contradictory. The correct interpretation:

> **Build the whole road, but activate it progressively.**

Phase A earns understandable rendering. Phase B earns exact economic
binding. Phase C earns `SURFACE_READY`. **Only then does the build cross
authorization.** The already-built post-auth machinery (RECON-1, INVOICE-1,
the rail adapters) then carries the live ceremony forward — the UI seat
does not need to invent economic semantics, only composition + UX + real
integration.

## A9c RED IS THE EXPECTED SIGNAL OF LEGITIMATE PROGRESS

`scripts/recon1-oracle-verify.mjs` check A9c encodes today's verified
truth: `SURFACE_MISSING`. Evidence must not quietly flip yesterday's
assertion when the UI lands. The lawful sequence is:

`A9c RED` → "surface now exists" → **independent exercise** → re-ruling →
`SURFACE_READY`.

A failing A9c after building the Review & Pay route is not a defect to
route around; it is the gate working. Ride the sequence, don't repaint it.

## THE AV-1.4c FENCE (read this before touching CI)

Main's **static** job carries an INHERITED red: AV-1 serve-bridge battery
1.4c ("crash header did not kill the server"), failing on main since before
`e4334e49` (runs 35260183137, 35262034709) — buzz-meter/e-cash territory.
The merge receipts establish that RECON's node/test/scan and all four
economic batteries are GREEN beside it.

**Fence law for every seat, especially the fresh UI session:** if you
encounter main's static RED, do NOT adopt it. It belongs to the separate
buzz-meter/e-cash lane unless YOUR diff actually touches
`scripts/buzz-meter/` or the serve-bridge path. The wanted first report is
not "CI is red; investigating 1.4c" — it is the seam map:

> "I found the Bux intake. I found the quote path. I found INVOICE-1 and
> RECON-1. Here is the existing UI. Here are the exact missing seams. Here
> is the first RED."

## LANE HOLDS (founder routing, standing)

bFUzZ → **HOLD** · economic oracle → **WAIT** · RECON builder → **done** ·
zGenealogy → **WAIT** · Gesture-D → **separate architecture boundary** ·
bKiMi/Buzz runtime → **separate operational family**. One active builder:
the Max zCode UI session on the real W@tch/bPay UX. No new economic lanes
at this concentration point.

## THE ENDING THIS IS ALL FOR

When the screen reads **Ready for you / Autonomi Preservation /
`try_autonomi.mp4` / `214,091,829 bytes` / Storage `X.XXXX ANT` /
Network fee `Y` / Quote expires `…` / Review & Pay** — nobody sends
"approved." The founder presses the button. Through the surface, with
wallet/Trezor, as Phase D's real gesture.

## HUMAN INTERACTION

NONE for this rider (docs only, ordinary merge, agent-side). The first
founder gesture of this road is the button press above — when Phase C/D
earn it.
