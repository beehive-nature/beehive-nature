# NO VPN DAY — dispatch 2026-08-31 (@be2ac57 + @edd02f9)

Founder's ask: the building wifi blocks the estate; can code on OUR side make the
VPN unnecessary? **Answer: yes — shipped and verified live from the founder's
laptop, on the blocking wifi itself.** CI green (33457807239).

## THE DIAGNOSIS (proven, not assumed)

- Every `.buzz` host: `000` (connection killed) · every `.dev` host: `200` —
  probed from the founder's laptop on the building wifi.
- DNS resolves skaists.buzz fine (129.153.202.144) — and
  `curl --resolve skaists.buzz:443:129.153.202.144` STILL dies.
  **The filter reads the hostname inside the TLS handshake (SNI) and RSTs on
  the `.buzz` string. Name-layer block.** No laptop setting fixes it; the only
  cure is roads that never say `.buzz`.

## WHAT LANDED

### Box (live, reloaded, verified)

- **The hive doors ride the clean hosts now.** `relay.skaists.dev` and
  `relay2.skaists.dev` answer browser visits with the hive door (same `@door`
  matcher idiom as the .buzz blocks: path `/` + `Accept: text/html*`).
  NIP-11, websockets, /api/*, /invite/*, /voucher/*, /compute/* never match
  and flow to the relay handlers — **verified from the blocking wifi:
  door 200/200, relay JSON intact, NIP-11 intact, wss handshake 101/101.**
- **Both doors display BOTH join roads** — primary `.buzz` (the community
  identity, ORIGIN RULING) + the clean road, a copy button each, one line of
  why. The doors were already road-portable (every URL relative except the
  displayed address), so this is the whole edit.
- Caddyfile backup: `Caddyfile.bak-20260831`. Config validated before reload.

### Repo

- **The doors are in-tree for the first time** (`surfaces/doors/skaists-buzz.html`,
  `beehivenature-buzz.html`) — they lived only on the box before. Repo is now
  the source of truth; deploy = scp to /opt/buzz/deploy/compose/door{,-bn}/.
- **buzz-studio ROAD MEMORY**: the opt-in probe learns which road answered
  and remembers it (localStorage `bz.road`); the next visit renders the
  working road first. Primary success CLEARS the memory — every network that
  can say `.buzz`, should. **Browser-proven on the real wifi**: initial
  `wss://skaists.buzz` → probe → `wss://relay.skaists.dev` + "remembered for
  next time" → reload → starts on the clean road.
- **buzz-directory** lists the clean web doors for both hives.
- **Registry**: both hive domains flipped DNS-PENDING → LIVE (read-back
  200/200 from the box — rows stale since the relay lanes). Doors registered;
  **87 = 87 = 87** (tree = deck = derived counts — the derive-from-tree
  machinery's first live fire, two new files, counts moved automatically).

## WHAT THE FOUNDER DOES DIFFERENTLY (the whole manual)

Nothing to install. On a filtering wifi:
- Hive doors: `https://relay.skaists.dev` / `https://relay2.skaists.dev`
- Buzz desktop app: Add Community → the clean wss address (on the doors)
- Everything else estate (skaists.dev, wallet voucher rail, compute) already
  rode clean names.

Only typing a `.buzz` URL directly still dies — that's the wifi's opinion of
the name, and no code of ours changes it. Every road now has a clean twin.

## Ops notes for the next seat

- SSH to the box: `ssh -i ~/.ssh/bnr_key ubuntu@129.153.202.144` — **the key
  file carries CRLF on the Windows laptop and libcrypto rejects it
  ("unsupported"); strip \r to a .lf copy first.** That was the blocker.
- Caddy/doors source of truth on box: `/opt/buzz/deploy/compose/` (bind-mounted
  into `buzz-prod-caddy-1`). Reload: `docker exec buzz-prod-caddy-1 caddy
  reload --config /etc/caddy/Caddyfile` (validate first).
- estate-source's drift check regenerates the hub in-place — the
  regen-commit-same-breath law fired AGAIN (third time): commit the regen
  before/with the gate run, or the byte-check reds.
