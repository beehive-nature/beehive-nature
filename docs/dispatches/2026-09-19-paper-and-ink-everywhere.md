# PAPER AND INK EVERYWHERE — the new-bee neutrals and the register control, estate-wide

**Seat:** Cowork (design + UI). **Date:** 2026-09-19. **Branch:** `cowork/shared-face-2026-09-19`.
Second step of the founder's makeover order, after the hub (`2026-09-19-hub-tree-of-life.md`).

## What changed

- **Neutrals only, one mechanical sweep** across 46 files (every surface that carried the
  old green-grey new-bee canvas, `register.js`, the music showcase under `docs/mvp-walk/`,
  and the 14 tests that pin the values): `#f6f7f2→#fbf7f0` paper · `#18362a→#0c1412` ink ·
  `#edf2eb→#efe9dd` well · `#e2eadf→#e9e2d5` lift · `#ccd7cf→#e6dfd2` line ·
  `#435f4e→#4a5f55` · `#52695b→#5a645e` (chosen so quiet text holds ≥4.5:1 on paper, card,
  well AND lift) · `#8a9e90→#857d70` control edge (≥3:1).
- **Semantic hues untouched.** Green still means living/verified wherever a page says so.
  The old green *action* colour (`#326b39`, 70 uses with mixed meanings) is NOT swept —
  it needs eyes per use; that is the next lane.
- **The register control, everywhere** (`register.js`, CSS only — behaviour, ids, order and
  `aria-pressed` unchanged): one row at 390px, a pill track, pressed pill filled (ink in
  new bee, magenta in raver, network cyan in cypherpunk), ✓ still says pressed without
  colour, emoji give way to the words.
- **watch.html**: the eyebrow no longer forces capitals (casing law).

## Fence

`surfaces/bdata.html` is NOT touched — zCode's active lane. It keeps the old canvas until
that lane pauses. Founder art under `surfaces/fleet*` carries none of these values.

## Gates (un-piped)

estate-check PASS · front-door suite 333/333 · estate-source on the committed tree ·
no-page-errors. Looked at, before/after at 390px: watch, profile, buzz directory, gallery,
onboarding, kandi.

**Receipt:** live pages at 390px — never the git state.
