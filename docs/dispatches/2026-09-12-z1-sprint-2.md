# Sprint 2 — finish the people and artist journeys; release the tested foundations

Founder has the four zCode seats available. This is the next assignment,
superseding their paused status only for the work below. Repository issue #10
is the handoff channel; seats are assigned, not claimed running until they
acknowledge. Read AGENTS.md, fetch, and use your own worktree. No shared-checkout
staging, force pushes, or weakened checks. Keep preserved Grok branches.

## Outcomes and current pins

- People: PR #57 remains at `f19513c1`. Finish the known translation and
  New bee readability gaps, then independent delta review and Astra integration.
- Artist: PR #35 at `8eac7117` is the single bloom receive/Keep/share engine.
  Resolve its release-readiness uncertainty by actually exercising the remaining
  receive, export/import and presentation paths. No second store or work identity.
- Autonomi: `9557aaeb` observer accepted, independently 50/50. Astra has already
  assembled release PR #58, added the suite to CI, and marked historical reports
  superseded. z1.c does not redo that work.
- Review: `8fcbd6d9` identity/uncertainty corrections accepted, independently
  39/39. Finish schema/validator parity and present an integration-ready core PR.

Upstream priority rechecked before this docket: x0x #505 and #622 remain OPEN,
latest requests unchanged. #505 still asks for the OCI workload against current
bootstraps; #622 still requires the eligible Leaf capture and separate peer
control. These four assignments do not fulfill or run those field experiments.
No laptop mesh/devnet nodes, new public services, signing, transfers, or production
upgrades/resizes/deletions belong to this sprint's agent assignments. Other Astra
retains Watch/3Speak/HLS and media-stack ownership.

## z1.a — people release patch

GLM 5.3 **Medium**, existing local implementation session. Own the PR #57
candidate branch and two people pages/page tests; sole shared-corpus writer.
Read #10 comment 5647899778 and z1.b's `b6d91011` review.

Finish F1/F5: key the three mid-beat support sentences and instrument heading/law
copy; populate all configured languages with machine-draft provenance. Finish
F4: New bee routine labels at least 14px, wrapping at 390px and RTL. Repair the
vacuous profile-language assertion. Preserve the accepted actions, illustrations,
Cypherpunk behavior and state retention. Do not rewrite shared chrome or CI.

Done: pushed descendant on #57; page/source/corpus and focused rendered checks;
screenshots of the repaired mobile/RTL content; no unkeyed newly scoped prose or
under-floor New bee labels. Meaning review remains explicitly separate from
machine translation coverage. Post head + short receipt to #10, then wait for
z1.b's delta findings. Do not start another page while this candidate awaits review.

## z1.b — independent release acceptance, useful work while a finishes

GLM 5.3 **Max**, fresh local read-only review session using the existing review
worktree only if clean; otherwise make a separate owned review worktree. Read
your `b6d91011` dispatch instead of replaying the long prior conversation.

Start on PR #35 at `8eac7117`: read its release contract and open limitations.
Run a real cold browser walk at desktop and 390px: bloom/credit first, explicit
Keep, duplicate Keep, share/receive view retention, pause/reduced motion, keyboard,
native file export and preview/import into an isolated second browser store,
merge with an existing collection, cancellation and denied storage. Check print
layout for the share card. Local fixtures and browser stores only. No media,
upload, ownership or JAMS compatibility claims; no native-language approval claim.
Inspect whether current shared chrome will regress the old candidate on release;
name integration conflicts instead of changing source. Return release blockers
with smallest reproductions and screenshots, or a bounded acceptance receipt.
Do not treat a human adoption observation or public social-card test as a bot pass.

When z1.a posts its new #57 head, review that DELTA next: F1/F4/F5, the repaired
profile assertion, mobile/RTL wrapping and retained accepted paths. Do not rerun
the entire earlier audit absent a relevant regression. Record separate exact
heads for the two reviews. Astra owns implementation fixes outside z1.a's scope,
integration and release; z1.b does not merge/deploy.

## z1.c — reproducible harness build candidate

GLM 5.3 **Medium**, fresh local implementation session; inherit the accepted
`9557aaeb` runbook and acceptance receipt, not the full correction transcript.
This is new work, not reopening the accepted observer. Own
`scripts/ant-extsig-repro/` and a new dispatch, with a fresh branch from fetched
main. Do not edit production-mirrored `ops/` or the root Cargo workspace.

Implement a small reproducibility runner that copies the canonical
`ops/ant-extsig` source into an isolated temporary build directory, applies the
reviewable dependency/workspace overlay and uses a committed candidate lockfile.
The banked ant-core revision is `969ed008d9cd39bbfe6466bc5ff7943914565994`;
ant-node `0.18.1` and ant-protocol `2.3.5` must be exact requirements, not caret
ranges. Prefer the previously obtained box lockfile if available and inspect
differences; otherwise resolve the candidate honestly, without claiming it
reconstructs the historical box lock. Do not create another maintained copy of
the harness source. The runner must refuse source/lock drift and avoid modifying
the canonical ops tree. Validate paths before scratch cleanup.

Done: ordinary local build tooling only, no executed node/devnet/listener; an
explicit Cargo graph receipt and successful locked check/build on a named host
when supported, plus a second locked resolution that leaves the lock unchanged.
Test missing/changed-lock and orphan-workspace cases. If the supported target
cannot build locally, return the exact first build blocker and a reproducer;
do not start Docker/WSL daemons or substitute metadata success for compilation.
No credentials or signing. Preserve the distinction between this build pin and
the STILL UNTESTED production auto-upgrade hold. Propose any eventual ops migration
as a patch for Astra's receipt/rollback process, not as a deployment.

## z1.d — finish the Review core contract

GLM 5.3 **Medium**, existing local implementation session. Continue from
`8fcbd6d9` and #10 comment 5647902093; do not repeat accepted identity repairs.
Own `crates/royalreview/`, its Lexicon, associated tests and dispatch. Root Cargo
membership/lock changes may be proposed in this candidate; Astra integrates them.

Compare schema/validator boundaries with official AT Protocol specifications:
UTF-8 length, TID bounds, strongRef URI/CID syntax, optional receipt shape and
Unicode/malformed inputs. Fix demonstrated mismatches and test boundary cases.
Keep digest syntax, claimed block identity and verified retrieved bytes distinct.
Document rejection-before-write versus ambiguous live-adapter error semantics;
the current fixtures prove neither network delivery nor an atomic multi-rail write.

Done: a draft integration PR with the tested core, exact head, locked tests/build,
format checks, and concise unsupported/live behaviors. No signing/OAuth adapters,
PDS, public posting or new parallel mirror engine in this assignment. The next
user-facing publishing slice follows acceptance of this contract.

## Astra and the operating rhythm

Astra owns PR #58 (observer integration + CI), PR #57 release assembly after its
delta acceptance, PR #35 integration/fixes after the cold receive report, and
Review/harness acceptance. The two user-facing release priorities are people
and the original family bloom; backend tasks run independently of those releases.

Claim #10 with seat, actual effort, owned paths and base. Return one concise
receipt: PR/head, changed behavior, executed tests, remaining blocker and dispatch.
Do not relay every command or loop on unchanged status. If the conversation starts
repeating planned actions without execution, stop and resume from the dispatch in
a fresh session. Report actual completion, never a planned push as a pushed commit.

Only z1.a edits the shared corpus; no agent edits workflow/shared chrome without
Astra's handoff. Existing #7 tracks translation meaning review. External product
links keep BNR open in a new tab. No new paid fleet. Use Low only for mechanical
receipt assembly, Medium for scoped implementation, Max for z1.b's independent
review; stop once the assigned acceptance checks pass.
