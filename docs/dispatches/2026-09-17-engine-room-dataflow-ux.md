# ENGINE ROOM DATA-FLOW UX — follow one file through the estate, one loop at three depths

ORDER: founder routing 2026-09-17 — a fresh High-effort zCode bee takes the Engine Room /
data-flow presentation role, separate from the two active payment/upload builders. Mission
verbatim: extend `surfaces/stack.html` with an interactive, evidence-grounded explanation of
Beehive's full data flow using physiology/homeostasis analogies; three depths (newbee/raver/
cypherpunk) change explanatory depth only; four circuits (payload/authority/evidence/value)
never confused; the Bux MP4 as the first source-backed example read mechanically; no second
dashboard, no privacy selector, no payment flow, no telemetry backend, no global view-toggle.

## WHAT LANDED

One new section in the Engine Room (`surfaces/stack.html#dataflow`), placed between THE SHAPE
and WHERE-AUTONOMI-IS — the estate's static anatomy now flows directly into its dynamic loop:

- **FOLLOW ONE FILE** — eight stages as native buttons (keyboard + 44px floor for free):
  intake → the boundary → working state → plan+quote → authority → the work → evidence →
  retain/release/feed back, closed by the ↺ loopback line ("waiting is a healthy state here,
  not a failure" — 5.2 s pulse, `prefers-reduced-motion` kills it).
- **Four circuits, never confused** — ● payload · ◆ authority · ◇ evidence · ○ value as
  glyph+word chips (D-1: greyscale-safe), per-stage combinations on every button.
- **Stage detail panel** — one paragraph per REGISTER (bee plain words / raver physiology /
  cypherpunk components+routes+status) above FOUR factual rows shared by all registers
  (what crosses · who may act · evidence+retention · what can fail) + the next-door link
  (which existing LIVE surface handles the next legitimate action: W@tch room, wallet—bPay,
  privacy lens, Autonomi door, record, canon comb — all relative links, all resolve 200).
- **ONE REAL FILE evidence card** — `surfaces/stack-dataflow-example.json`, GENERATED by
  `scripts/build-stack-dataflow.mjs` from the committed intake receipt (never hand-typed):
  214,091,829 bytes · sha pin 338b486874f6…44207e (short form; the full digest stays in the
  hex-law-marked receipts) · 3318×2132 · 52.488 s · playback PASS 7/7 · the founder-banked
  STATE BOARD as nine chips (verified / in review / not yet / by design). The quote row
  renders "in review" with NO invented ANT figure while PR #113 sits unmerged; the builder
  picks the real figure up from the quote receipt when it lands.
- **bData/zbData honored as written** (docs/CONTRACT.md: bData = public chunks, zbData =
  private encrypted bytes) with the two refinements the mission pinned: public ≠ unencrypted
  in transit, private ≠ zero-knowledge; no implicit private→public promotion; `zData` left
  UNASSIGNED — the page never invents a third category.
- **The constant line, all registers**: depth changes explanation only — never permission,
  privacy, or payment; a recorded explanation, not live telemetry.

## CLAIM → EVIDENCE → BOUNDARY NOT CROSSED

- **CLAIM: register switching is presentation-only.** EVIDENCE: `e2e/engineflow.browser.mjs`
  proves switching bee→raver→cypherpunk changes the paragraph key while the factual rows are
  byte-identical constants, fires ZERO network requests across all switches + all eight stage
  selections, and leaves localStorage untouched except the register preference itself.
  NOT PROVEN: nothing else on the page (the pre-existing organ probes still probe once at
  load — untouched, per the preserve-existing-probes order).
- **CLAIM: the evidence card is mechanical.** EVIDENCE: the e2e suite re-derives bytes,
  duration, verdict, dimensions and the sha pin from `docs/dispatches/
  2026-09-17-watch-try-autonomi-intake.md` INDEPENDENTLY and matches them against the DOM;
  the builder refuses to run if any receipt field drifts. NOT PROVEN: the quote figure —
  correctly absent while its only source (PR #113) is unmerged.
- **CLAIM: honest degradation.** EVIDENCE: with the artifact route aborted, the card renders
  the degrade line + a link to the receipt, invents no numbers, zero page errors.
- **CLAIM: the loop speaks 29 tongues.** EVIDENCE: 112 new `flow.*` keys × 29 cells in
  `surfaces/lang-corpus.json` (1752 → 1864 keys), written by `scripts/stack-dataflow-corpus.mjs`
  (idempotent, refuses incomplete cells); the nine state-board chips mirror the receipt's own
  vocabulary — record of origin, never translated (identical cells, kandi precedent); ru
  rendering proven corpus-exact in-browser; `lang.js` corpus bust bumped v24→v25.
- **CLAIM: keyboard, motion, phone.** EVIDENCE: Tab reaches stages unaided, Enter opens;
  reduced-motion disables the pulse with the section still interactive; 390×844 renders with
  no horizontal spill and ≥44px touch targets; four shots banked in `e2e/shots-engineflow/`
  (bee/raver/cypherpunk at 390 + cypherpunk at 1280).

## GATES (local, this branch)

engineflow.browser 28/28 · estate-source 11/11 (1864 keys, 28 tongues, corpus English
byte-matches the page) · estate-check PASS (95 counted · 104 listed · 3 orgs · hub in sync —
no new surface, no registration ritual owed) · i18n-coverage selftest + floors PASS (floors
hold; the 118→~230 keyed growth on stack.html is headroom, not a ratchet taken) ·
no-page-errors 104 walked / 0 errors · university-smoke 87/0 · secret-scan tree clean (the
sha pin is deliberately short — 12+6 hex, under the 48-run law, no marker owed). INHERITED
NOT TOUCHED: main's AV-1 serve-bridge static red (buzz-meter territory, per the AV-1.4c
fence — this lane's diff reaches none of it).

## THE A9c NEAR-TRIP (banked, fix-forward)

The first push tripped RECON-1's A9c tripwire: any `surfaces/*.html` carrying the literal
route string forces a SURFACE_READY re-ruling — and my cypherpunk authority paragraph NAMED
the chartered step in prose. The tripwire is deliberately blind to intent, and that is its
value; the cure was not to argue but to stop impersonating the route name in a surfaces file:
the copy now says "the review-and-pay step (… no UI route exists today; an estate tripwire
guards the day one appears)" — the fact stands, the landmine stays armed for the real surface
(the bPay UI seat's, not this page's). recon1-oracle-verify GREEN again; lesson banked: prose
under surfaces/ names steps, never route strings guarded by tripwires.

## MERGE-SYNC (2026-09-17, before merge — the first wake trigger fired DURING review)

While this PR sat, #113 (Phase A invoice) AND #114 (Phase B chooser) merged — main moved to
ac4a783d and this branch conflicted on exactly the predicted shared file (lang-corpus.json).
Sync performed under the ordinary discipline: corpus conflict resolved by taking main's
corpus and re-running this lane's writer (1789 + 112 = 1901 keys, both sides intact);
lang-corpus bust bumped v25→v26 (both sides changed the corpus). And the founder's FIRST
listed wake trigger — "#113/Phase B lands → Engine Room learns about the real quote/policy
evidence" — is already satisfied: `scripts/build-stack-dataflow.mjs` now parses the LANDED
quote receipt (markdown table, read at source: total ANT row, chunks row, payment_type row),
the card renders the REAL figure (4.245934921875 ANT · 56 quotes · wave_batch) and the
ANT-quote chip upgraded NEXT→verified (newer evidence upgrades the RENDER, never the frozen
intake record). Stale prose retired: "(PR #113, in review…)" → "(PR #113, merged 5e1f3cf6)"
and "the chooser … Phase B" → "PR #114 (merged)" — tongue-neutral reference tokens, names
never translated. e2e assertions FLIPPED from no-invented-figure to
mechanical-from-quote-receipt (30/30). A9c re-checked on the merged tree: GREEN, no
surfaces/*.html carries the route string — the landmine stays armed for the real surface.

## BOUNDARY NOT CROSSED

No edits to PR #113's files (wallet.html, bpay-invoice.*, its dispatch/receipt/e2e) — the
boundary/quote/authority stages LINK to that work and mark it in review. Shared files
touched additively only: lang-corpus.json (+112 keys), lang.js (one-line bust bump),
tests.yml (one new node-job step) — whoever of #113/this merges second rebases one line.
No privacy selector was built (the chooser stays Phase B with its owner). No new surface, no
estate.json row, no atlas/deck change. No production operations, no wallet signing, no
storage uploads, no live telemetry — the diagram animates nothing it did not record. Proposed
architecture stays labeled inside cypherpunk prose; only receipts claim verified.

## CHANGED

surfaces/stack.html (+226 lines: section + CSS + presentation-only script) ·
surfaces/stack-dataflow-example.json (new, generated) · surfaces/lang-corpus.json (+112×29
cells) · surfaces/lang.js (v25) · scripts/build-stack-dataflow.mjs (new) ·
scripts/stack-dataflow-corpus.mjs (new) · e2e/engineflow.browser.mjs (new, 28 proofs) ·
e2e/shots-engineflow/ (4 shots) · .github/workflows/tests.yml (+1 step) · this dispatch.

STATE: complete on branch `zcode/engine-room-dataflow-2026-09-17`, PR open, all local gates
green. NEXT OWNER: authorized merger (after #113 or before it — independent either way).
HUMAN INTERACTION: NONE.
