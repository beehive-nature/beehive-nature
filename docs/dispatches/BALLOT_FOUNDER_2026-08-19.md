# BALLOT — Founder rulings requested: kernel G1–G6 · design D-F1–D-F6
**From:** Cowork seat · 2026-08-19 · **Status:** OPEN — awaiting founder's words
**Naming correction, first:** the Cowork chat briefly aliased the kernel gates as "⚑F1–F6." That alias is retired here. Kernel gates are **G1–G6** (canonical per `docs/SPRINT-FINISH-LINE-MAP.md`, in-tree). The design seat's flags are **D-F1–D-F6**. Two ballots, separately counted, per the design seat's correct insistence. This dispatch is the queue trigger: when the founder's words land, zCode's G2 work and the design seat's D2 open on it.

---

## Ballot A — kernel gates G1–G6 (unblocks: zCode G2 commit, sprint phases)

**G1 · Root entropy — recommend RATIFY 24 words.** Implemented, tested (26/26), 12-word input refused with an honest explanation. Unfixable after the first real enrolment.

**G2 · Wire labels — recommend RENAME NOW to match Vocabulary Law 6.** Engine wire labels read `BDID-v1/…` and `bdidrec`; product vocabulary is bzDiD; the seam is pinned in-tree twice (26-suite + ported e2e), so nothing renames silently. Cost measured by zCode: rename = two files + regenerated pinned vector (~1 hr); freeze = one paragraph on the wall. Cheapest this will ever be — before any mainnet derivation makes the bytes load-bearing. `BSAFE7-THP-v0` unchanged (device name didn't move). Either answer is legal; not answering is the only wrong one.

**G3 · Optical framing — recommend RATIFY BC-UR.** Mature multi-vendor standard since 2020, fountain-coded, Irvine doctrine (boring and shared). bComb and decimen keep their documented complementary lanes.

**G4 · Context registry — recommend RATIFY the shape, seed with two.** Contexts are canonical strings governed like namespaces, ≥8-byte tags; seed set `bnr.b` (kernel), `skaists.b` (community); minting new contexts is a raid act. Enumeration stays open; only the shape locks.

**G5 · Root-door matrix — recommend RATIFY as built.** PRF passkey ⇒ root from PRF (two doors, one root); no PRF ⇒ fresh-entropy root, phrase is the only root door; Trezor rung ⇒ device seed, phrase skipped (founder ruling 2026-08-14).

**G6 · `Enrolment.code_hash` — recommend DEMOTE to local write-down check.** The phrase is derivational (recovery needs no service alive); the stored hash is UX courtesy, not credential. Small API note in `crates/onboarding`.

**Founder's words (Ballot A):** ____________________

## Ballot B — design flags D-F1–D-F6 (unblocks: D2 — hub + TRADE wing on macro tokens)

**D-F1 · Pale fills — recommend RATIFY.** Any fill-step under 3:1 vs surface carries a 1px `--line` hairline as secondary encoding. Same law as "human identity always carries glyph or label," applied to fills.

**D-F2 · 7-step depth — recommend RATIFY "labeled charts only."** 5 stays default everywhere; 7 ships only with direct labels or patterned fills (inner CVD pairs 6.5–7.8 are legal per Law 1 only with secondary encoding).

**D-F3 · ≥15 floor binds identity sets only — recommend RATIFY this reading.** Categorical colors are identities and take the floor; ordered ramps encode by position and magnitude and run 10–14 by construction. (A reading of Law 1, not a fact — it needs the founder's word, which is why it's here.)

**D-F4 · One leaf — recommend RATIFY `#7ddf8f`.** Validated review.html lineage wins; the hub's private `#7ec850` re-derives in D2. Private constants retire; validated lineages rule.

**D-F5 · TRADE retirements — recommend RATIFY as listed.** `#9FFF3D`, `#5a6478`, `#050508`/`#0a0a0f`, `#cfe8d6` retire into status-works, ink-dim, surface-0/1, ink. D2 shows the diff door by door. The rave energy survives in the mandala decoration layer, which decoders and doctrine both ignore.

**D-F6 · Comb-6 cell names — recommend: NO new names; use ratified law.** BNR-LAB-1 L-2 (ratified 2026-08-18) already fixed the axes: **Openness · Air-gap · Custody law · Build · Art (★, founder-scored) · Ecosystem fit**. The design seat's illustrative six (CORRECT/CLEAR/…) would fork the vocabulary. Only genuinely new decision: short cell-forms for tiny renders — recommend **OPEN · AIRGAP · CUSTODY · BUILD · ART★ · FIT**. Geometry, seal, human-scored star stand as drawn.

**Founder's words (Ballot B):** ____________________

---

*Suggested one-message ratification, if the recommendations all read right:*
> **Ballot A: ratify G1–G6 as recommended (G2 = rename).**
> **Ballot B: ratify D-F1–D-F5 as recommended; D-F6 uses the L-2 axes, cell forms OPEN / AIRGAP / CUSTODY / BUILD / ART★ / FIT.**

*On landing: zCode files this with the founder's words appended verbatim under `docs/dispatches/RULING_BALLOTS_2026-08-19.md`, queues G2, and pings the design seat that D2 is open. Don't trust this ballot either — every recommendation cites its receipt. Verify.* 🐝
