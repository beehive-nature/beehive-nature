# LANE WIRING — THE MASTER DESIGN PASS · 2026-08-28

**Seat:** z2.1 · **Source:** `Downloads/Master Design Pass with wallet buzz v1.zip`
(4 `.dc.html` screens + tokens.css; the `.dc` runtime fetches are preview-only
and ship nowhere — every bind is BUILD-time) · **Status: CLOSED — all four
surfaces DONE at their live URLs, continuous, in order.**

## 1 · HUB — DONE (main `a82395e`)

`surfaces/index.html` regenerated by a rewritten `scripts/build-atlas.mjs` in
the master design: **three org houses** over the eight families, grouped by the
registry's own org axis (@01ed46d authority); per-org counts computed at build
(51/23/5 · domains · families); hero 79; org pills + family pills; search stays
the ONLY runtime script (now hides empty houses, .dc copy); the three
provenance links kept; fleet sentence stays COMPUTED. **PRESERVATION: the
hex-band masthead is lifted BYTE-TRUE from `surfaces/doors/index.html` at build
— 12,178 bytes, proven by string containment; the lift self-checks brace
balance (a first line-picker orphaned a continuation rule and the open brace
ate the stylesheet — the gate caught it, the lift was fixed structurally).**
Gates: design 12/12 · THE METHOD median FCP 196ms / 12 req · estate-check
PASS · render smoke (3 houses / 8 fams / 87 rows / 147 hexes animating /
search 1-hit) · CI read on lane and main · live verify + before/after 390px.

## 2 · WALLET — DONE (main `f1e8206`)

`surfaces/wallet.html` restyled to the master chrome as a PURE restyle — same
DOM, same ids, same engines; **`WALLET_PROFILE` remains THE head config home,
untouched; the design re-hard-wires nothing.** Style block in the master
language (grounds #06110C/#0C1412/#0E1A12, biomass the one chrome accent,
hex-dotted uppercase section heads, gradient h1, ink balance numbers — gold
retired from non-b amounts); breadcrumb + kicker chrome added; hero attrs on
the live Vaulta read. **The receipt ladder lands WORD-FOR-WORD** — all five
stage lines + the SUBMITTED/CONFIRMED badge texts — as the composer's
presentation; the design's walker is not shipped (its own copy says the build
seat wires the real lanes; the real lanes already speak in the composer's live
receipt line). The design-acceptance allowlist grew the wallet's first-party
vendored riders by exact path (bzdid-key, bnr-sign/keys/qr, bip39-wordlist,
vault.js, arweave.js). Gates: design 12/12 · METHOD 224ms · **matrix 11/11 ·
adapter 28/28 (JWK wall intact) · arweave 22/22** · CI read · live verify +
before/after 390px.

## 3 · BUZZ ROOM — DONE (main `6c84591`)

`surfaces/buzz-studio.html`: the ROOM head block carries the **NEW FACT — the
room is LIVE** (`state:'live — as of 2026-08-28'`) plus the **web door**
rendered from the block as a standing invite; chrome to the master grounds +
crumbs + kicker + section heads; **the single wire-cyan (tokens --ai
#45C2DC) RESTORED and verified computed on the wss join line** (the screen
dropped it; ours never will). Zero page-open network re-proven (external
request count 0); probe stays opt-in; TURNKEY ROOM block intact; CRDT engine
untouched, re-proven two-tab (A=5 B=5). Gates: design 12/12 · METHOD 184ms /
4 req · CI read · live verify + before/after 390px.

## 4 · BN SYSTEM — DONE, the optional fourth (main `ef33a40`)

`surfaces/design-system.html` — the estate's living style guide, **full
citizen**: registry row (family beehivenature, org beehive-nature), counts
recomputed **89 listed · 80 counted · byOrg 52/23/5 (sum 80)**, review deck
80/80, atlas re-rendered, estate-check PASS. The page demonstrates the
two-axis palette law chip-on-ground, the depth ladder with the REGISTERED
derived well, senary rhythm, the type specimens (payload casings verbatim),
the static 91-cell mandala, menus/badges/controls, and the guarantee-door
specimen. **PRESERVATION: THE COMET lifted byte-true from `surfaces/kandi.html`
(CSS + canvas + the whole IIFE) and the hex band byte-true from doors — both
proven by containment; the comet verified PAINTING live via getImageData, the
band's 147 hexes animating hexpulse, reduced-motion still intact inside the
lifted code.** Gates: design 12/12 · METHOD 224ms / 5 req · CI read · live
verify + shots.

## Fences held

No CI files touched. No other surfaces touched (review.html's roster + the
atlas/registry rows are the citizen wiring itself). Cache keys unchanged —
no rider bumped, none needed. Registry: no new rows for the three
replacements; the 4th's row is its ordered citizen wiring.

## Receipts index

Live URLs: skaists.dev/surfaces/{index, wallet, buzz-studio, design-system}.html
Shots: `e2e/shots-wiring/` — hub/wallet/buzz before+after live 390px, design-system live 390px.
