# W@TCH — UX PASS 1 (founder: "no one has touched the UX so full attack")

**Seat:** Cowork (design + UI). **Date:** 2026-09-19. **Branch:** `cowork/watch-ux-2026-09-19`.

The empty room was all dead ends. After this pass (all in `scripts/tmp/watch-ux.mjs`, asserted,
idempotent, **no new strings**):

- **The screen has one real action.** The round play mark that played nothing steps aside where
  no room is hosted; in its place a filled, 52px control into the music room — the one thing that
  works today — speaking with that room's own words (`music.beeLead`, all 29 tongues). Arrow
  mirrors in RTL; focus ring in screen ink.
- **No ticker of dashes** where there is no room to report on.
- **The house hand** (burti, same-origin, names only) sets the brand.
- **The phone header is two rows, not four:** brand + language on one row, the register control
  under it; the room's own nav scrolls sideways (44px targets) instead of wrapping.
- **Raver gets light:** a slow aurora in the screen (magenta / network cyan / human purple),
  only under `prefers-reduced-motion: no-preference`.

**Coverage floor 45 → 44, on purpose:** the dash ticker (2 keyed leaves) leaves the static page, the screen action (1) arrives.

**Gates (un-piped, browser proofs before the push):** W@tch 29/29 (29 tongues × 3 views fit 390px,
enlarged text fits) · front-door 333/333 · no-page-errors · estate-source · coverage floors.

**Next passes (named):** the pass flow and chat on a LIVE room host (needs one to judge against);
desktop composition; the Privacy & access panel as plain rows.

**Receipt:** the live page at 390px — never the git state.
