# z2.b R2 independent review

Reviewed pin: `e1ca64b1c72b6db1f087c36391246da54045ecf0`.
Review seat: Codex (Astra), isolated detached `wt-astra-watchpay-review`.
Builder worktree untouched.

## Verdict

The two R1 findings are resolved in this offline candidate. Integration remains blocked by hosted Rust formatting; this is not authorization for z2.c, device access, signing, broadcast, payment, uploads, or deployment.

`ValidatedPlan` now has private fields and immutable getters, including its derived approval ceiling. The prior public mutation API is eliminated. Receipt validation rejects impossible gas usage and fee evidence before outcome classification; ledger reconciliation repeats the validation. Missing evidence retains reservations. Zero-price evidence remains allowed only within the signed envelope.

Independent command: `cargo test -p watchpay --locked --offline --quiet`, run under WSL in the detached review worktree. Result: 65 passed, zero failed (14 + 11 + 3 + 15 + 13 + 5 + 4); the three upstream byte-parity tests pass. The original local R1 probe file was retained with a non-Rust extension before checkout, so it did not shadow the candidate's tracked regression file.

Hosted job [103610324617](https://github.com/beehive-nature/beehive-nature/actions/runs/34714914113/job/103610324617) has conclusion failure; its only failed step is `Check formatting`. No green integrated commit or live payment proof is claimed. Clippy and secret-scan success were reported by the builder; they were not independently rerun in this review.

## Bounded next order

Use the existing z2.b build session. Apply rustfmt to watchpay using the repository CI toolchain, inspect the diff for formatting-only changes, and run the CI formatting command plus the 65 offline tests and clippy. Commit with canonical attribution and push a descendant without force. Report the new SHA and hosted checks on that SHA. If formatting requires changes outside this crate, identify their origin before widening scope. Keep all live-action gates closed.
## Final formatting gate — accepted

Final pin: `76b23661513dd6d33b81f465a3bd958499271864`, direct descendant of `e1ca64b1`.

Independent verification: applied `cargo fmt --all` to the previously reviewed e1ca64b1 source in the isolated reviewer worktree, then compared it against the final pin using Windows Git. `git diff --quiet 76b23661 -- crates/watchpay` returned zero: the formatted source exactly matches the candidate. Only the 16 watchpay Rust files changed between commits. This carries forward the independently observed 65 offline test passes without claiming an additional local rerun. The WSL Git comparison initially failed because the worktree metadata uses Windows paths; the subsequent Windows Git comparison completed successfully.

GitHub check-runs queried at the exact final SHA: `test`, `node`, `static`, and `scan` all completed successfully. Founder author, zCode committer, and parsed Co-authored-by trailer verified.

Verdict: z2.b offline slice accepted at this pin. Formatting blocker closed. No merge, hardware integration, real signing, broadcast, funded upload, or deployment is performed or authorized by this acceptance. Those require a separately scoped next implementation and review.
