# bSymposium three-temperature adapter — 2026-09-11

Seat: Grok / Cursor cloud agent. Lane: adapter #2 after bEarth #45
(the live order on this beat: bEarth → Symposium; WELLness / Hexagon
remain later). Branch: `cursor/bsymposium-three-temp-9e07`.

David Irvine / x0x #622 is still the standing backend priority. This
seat was assigned the Symposium presentation adapter; no backend, mesh,
or measurement claim is made here.

## What changed

Same dual-receipted MAHA × bFood discourse. First paint is what changed.
No new health-outcome sentence was added. Effect claims stay Axis B and
stay off the New bee arrival.

## Visual QA poke

`http://127.0.0.1:8765/bsymposium.html` with `surfaces/` as the server
root — the same poke that failed New bee on bEarth #45 when `tour.js`
loaded register/lang from `/surfaces/`.

Cure, copied from that PR:

- `tour.js` sibling riders (`register.js`, `lang.js`, `rails-badge.js`)
  resolve from `document.currentScript.src`. Tour-bar hrefs still use `R`.
- Page-owned `#bregbar` with `data-register-host` and `data-language-host`
  so `#bregctl` and `#blangctl` paint on first-paint chrome.
- Inline `[data-reg]:not(body){display:none}` plus the matching revert,
  so the cypher masthead (Federal Register / EO / Axis B) cannot FOUC
  onto New bee if register is late or 404s.

## Graded first paints (Chief PASS, implemented as written)

- **New bee:** calm sentence (“Two projects care about the same plate…”),
  takeaway lead (“Not a winner — where they strengthen each other.”),
  quieter support (leftover questions wait on checkable evidence), one
  choice (**See where they meet**), and **Go deeper**. ZERO dual-receipt
  tables, Federal Register walls, EO numbers, five co-creations list,
  Axis B fence prose, ◐ / settlement jargon.
- **Raver:** two voices at one shared plate in a soft room (reduced-motion
  = one still frame), feeling line (“What if the argument about food
  became a listening?”), one tap (**Sit with both sides**). Consciousness
  (“Homeostasis — each protocol making the other stronger”) is layer 2
  only, then the New bee takeaway stack.
- **Cypherpunk:** today’s full instrument on contact — charter, seven
  dual-receipt rows, co-creations, record, cite-or-silent. Sources use
  `data-view-disclosure` and default open in this view.

Footer keyed leaves (`s.bsymposium.name`, `law.hive`) stay laid out so
the first-paint floor of 10 does not drop. Dial-grade / Axis B / “don’t
trust” clauses ride `data-reg="cypherpunk"`.

## Language

Thirteen `bsymp.*` keys were machine-drafted across English plus the 28
docked tongues and marked ⚙. `plate` means dinner plate / food, not a
printing plate. `listening` is the act of hearing. Meaning review is
still owed. Existing `h.101`–`h.109` headings stay on the instrument.

## Verification named here

- `node --test e2e/bsymposium-views.test.mjs e2e/register.test.mjs`
- `node scripts/estate-check.mjs`
- `node e2e/estate-source.mjs`
- browser first-paint screenshots of the three registers at the 8765 poke

## Re-poke receipts (same day, 8765)

Served `surfaces/` at `http://127.0.0.1:8765/bsymposium.html`. Confirmed
`/surfaces/register.js` is 404 on that poke (the old alias) and
`/register.js` + `/lang.js` are 200. Puppeteer-core against system
Chrome, `localStorage` cleared, 1280×800:

- New bee: `.sub[data-reg=cypherpunk]` `display:none`; instrument
  `display:none`; above-fold ZERO Federal Register / EO 14212 / Axis B /
  five co-creations / ◐; `#bregctl` and `#blangctl` painted; calm +
  takeaway lead + See where they meet.
- Raver: feeling line on; Homeostasis off; instrument hidden.
- Cypherpunk: instrument + cypher masthead on; sources default open;
  charter / seven rows / co-creations / cite-or-silent intact.
- Switch back to New bee: cypher masthead stays `display:none`.
- See where they meet: two commons cards, still no table / EO.

23/23 poke assertions (script labelled 15/15 at the banner; every named
assert printed PASS). Browser walk New bee → meet → Raver → Sit with
both sides → Cypherpunk → New bee matches. Beat memory keeps the meet
layer when returning to New bee after that tap — same contract as
forge/room.

Skipped this beat: fleet attestation of the new keys, WELLness / Hexagon
adapters, any health-outcome rewrite, live Federal Register re-fetch
(the 2026-08-21 link-status table is unchanged).
