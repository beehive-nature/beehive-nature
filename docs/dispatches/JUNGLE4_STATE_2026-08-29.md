# JUNGLE4 STATE — budget-rider recon (read-only, 2026-08-29)
**Rider:** keying batch HELD until the 04:10 reset; this is the low-cost findings pass.

- **Chain:** jungle4 alive — chain_id `73e4385a…`, head block 284,108,284 (jungle4.cryptolions.io).
- **banchor22222:** still funded — `core_liquid_balance: 70.0000 EOS`; **the POSTOP-era contract still sits deployed**: code_hash `e04824f18693bf86d002f9796595ddfa4b0a32a3a5a525f46100037a176dd0d0` PUBLIC-CONSTANT (on-chain, anyone can verify). The old experiment is not lost — it is resident.
- **Faucets:** grease + eosn both unreachable again (000) — account-creation paths remain DEAD, unchanged from LANE_B's receipt. No cc1 reply to QUESTION-TO-CC1-JUNGLE4-METHOD yet.
- **Consequence for the two queued proofs:** (1) memo-binding proof stays at its two one-gesture doors — the composed mainnet dust self-send (0.0001 A → kingbeelovis, memo `apoll-proof-1`, paste-ready in RECEIPT-CHAINPOLL-MEMO-BIP352-2026-08-29.md) or the founder-browser captcha create; (2) permission experiments need an account whose key we hold — blocked on the same doors. Nothing new to execute without a human gesture; nothing wasted — the resident code-hash is a new permanent fact.

## BUDGET-RIDER ADDENDUM — the acceptance read (2026-08-29, later)
Founder tightened both proofs' acceptance (memo: correct/wrong/none — three tx ids; permissions: the refusals are the proof). Executed the one cheap check available: kingbeelovis mainnet history (greymass, last 40 actions) — **the composed `apoll-proof-1` dust self-send has NOT landed** (history shows live DEX trading only). Both proofs remain BLOCKED on signing authority: memo needs the founder's wallet (or cc1's method — reply still owed); permission experiments need an account whose key the estate holds (faucets re-verified dead tonight). At low effort, STOP is the correct move — no interpretation, no run, nothing claimed.
