# RULING — A-NATIVE FOR THE B PROTOTYPE · JUNGLE-FIRST DEPLOYMENT (founder word)

**Date:** 2026-08-22 (late session) · **Ruler:** founder, verbatim:

> **"we will use Vaulta's A native token for our B prototype since we are not deploying
> it to mainnet until after fully building and testing smart contracts on jungletestnet"**

Two laws in one breath, both standing from tonight:

## 1 · A-NATIVE FOR THE B PROTOTYPE

The prototype's metering and settlement rail **denominates in Vaulta's native A token**.
This AFFIRMS and strengthens the B1 A-first law — it does not open Q-2/MX-2: the `b`
path stays exactly as gated as it was. The `b`/Respect model's own law is unchanged
(`crates/b-token`: `b` is kernel-side energy, minted on ResourceProof, burned on use,
**never an EVM token, never bridged, never gas**). The ruling means: while the economy
is prototype, **A carries every metered unit** — the b-token's on-chain debut, when it
comes, follows the same Jungle-first law below.

## 2 · JUNGLE-FIRST — THE DEPLOYMENT LAW

**No mainnet deployment of any BNR smart contract until it is fully built and fully
tested on the Jungle testnet.** Mainnet arrives only after the Jungle pass, on founder
word (gate **MX-7** in SPEC-BMESHASI-EXCHANGE-1).

## 3 · The test bed — verified live TONIGHT (keyless, receipted)

The lane was already prepared (`POSTOP_COWORK_JUNGLE_ACCOUNT_2026-08-08`); tonight's
reads (pinned endpoint `jungle4.api.eosnation.io`, HTTP 200):

| fact | 2026-08-08 postop | **2026-08-22 tonight** |
|---|---|---|
| account `banchor22222` | created, owner/active | **live**, same two permissions |
| A balance (`core.vaulta`) | 100.0000 A | **100.0000 A — untouched** |
| core balance | 100.0000 EOS | **70.0000 EOS** (~30 went to RAM) |
| ram quota / usage | 5,495 / 3,446 (C1 blocker: buy RAM) | **152,279 / 109,681 — the C1 recommendation was executed** |
| contract code | none | **none yet** (`code_hash` empty) — the bed is staged, not sown |

The A-denomination rail therefore already exists on the test chain, funded and idle.
Standing laws carried forward unchanged: the faucet human-proof for this identity is
**count 1 and must not go higher**; keys are testnet-only throwaways and **never enter
this public repo**.

## 3.5 · AMENDMENT (founder word, same night): the two tracks

> **"A again will be our prototype for Vaulta mainnet and use Vaulta's jungletestnet
> for b build"**

Sharpened into two explicit tracks:

- **TRACK A — the A prototype, Vaulta MAINNET-facing.** A is the mainnet token; the
  A-denominated lane (keyless reads, the meters, the exchange's settlement rail) faces
  mainnet by design — it already does, keylessly and read-only (the bANTfarm, the DAO
  dashboard, tonight's live dials). No contract deploys ride this track.
- **TRACK B — the b build, JUNGLE TESTNET.** The b-token contract work builds and
  tests on Jungle (`banchor22222`, RAM bought, A funded); MX-7 unchanged and absolute:
  b reaches mainnet only after the full Jungle pass, on founder word.

The two tracks never blur: **A flows on mainnet, b is forged on Jungle.**

## 4 · What this opens / what it does not

- **Opens:** contract build + deploy lane onto `banchor22222` (RAM already bought),
  A-denominated metering tests against the bmesh-meter/bmesh-ram models, and the
  bMeshAsi exchange's settlement tests — all on Jungle, all before any mainnet breath.
  The read instrument for the A-side treasury is already live and keyless:
  **`surfaces/bantfarm.html`** — its Vaulta reader tries `core.vaulta/A` first by
  design (a preference the ruling promotes to law), and the metering test lane
  inherits its proven three-host failover set. The ruling note rides the surface.
- **Does not:** open Q-2/MX-2 (b on the metering path — still gated), touch the
  b-token's kernel-side law, or authorize mainnet anything (MX-7 now names that gate).

**Lineage:** [[DISPATCH_COWORK_B1_BUZZ_A_METERING]] (A-first + self-funded) ·
[[POSTOP_COWORK_JUNGLE_ACCOUNT_2026-08-08]] (the bed, the endpoint pin, the human-proof
law) · [[RECEIPT_ZCODE_R3_BMESH_RAM_2026-08-22]] (the stock-market model this rail will
be tested against) · SPEC-BMESHASI-EXCHANGE-1 (settlement §3, gates §6 → MX-7).
