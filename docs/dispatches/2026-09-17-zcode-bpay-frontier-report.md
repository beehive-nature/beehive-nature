# bPay economic frontier — the collision-checked report + the claim (zCode economic-systems seat)

**The routing** (founder, 2026-09-17): this seat = bPay/bMeter/POS/Wallet
economic-systems seat; WAIT preserved on Gesture D's next lawful pass and
VV-1 builder GREEN; not globally idle. Mission: frontier-discovery on the
general economic artifact lifecycle
QUOTE→INVOICE→AUTHORIZATION→SETTLEMENT→RECEIPT→RECONCILIATION, generic
primitives first, zGenealogy as first demanding consumer, nothing
implemented into zGenealogy, no Jungle4, no VV-2, no Gesture D resume, no
IF-1..IF-4. First return: a frontier/collision report + smallest
non-colliding claim; if GREEN/YELLOW, claim and proceed.

**SYNC state observed**: main @22af64fb — Gesture D executed and STOPPED AT
BOOT (door flock self-deadlock; builder charters the fix — this seat's WAIT
trigger unchanged); Ceremony M fully closed (AV-1/2/3 all D; /v1/pricing/
admit freshness law deployed; admin charge parks on door-unreachable);
R20 charter banked on codex/z2b-bpay-rail (F1 domain tag + F2
canonicalization + durability + parent-scope; writer re-frozen there);
IF-1..IF-4 = the Gesture-D integration-findings queue on the disposition
branch, standing note verbatim "None of IF-1..IF-4 authorizes
implementation" — respected.

**The frontier verdict** (full map with file:line evidence in
`docs/agents/BPAY-ECONOMIC-LIFECYCLE.md`, landed with this report):

- **QUOTE — strong but fragmented**: QuotingDesk single-use quotes (ttl
  600s, desk-side), the deployed AV-2 freshness law (rate_set TTL 300s at
  the gate admission boundary), bpay-rail's `PinnedOffer` (no TTL field),
  banchor `PLAN_TTL` — three TTL regimes, no unified vocabulary.
- **PRICING COMMITMENT — exists as fragments, unnamed as one thing**: the
  buzz-meter quote consumed single-use, watchpay's sealed ceiling, bpay-rail
  manifest snapshots — and the frozen VV-1 law, which IS the general
  principle (self-sufficient, immutable, snapshot execution). This frontier
  generalizes it off-chain without touching the frozen lane.
- **INVOICE — ABSENT**: zero invoice structs anywhere on main; the only
  invoice-shaped UI decodes OTHER people's BOLT-11 strings; receipts exist,
  invoices don't. The cleanest empty stage.
- **AUTHORIZATION — deep but fenced**: bpay-rail R19 signed capability
  binding is another seat's active lane (R20); the economic boundary this
  frontier owns is the invoice→bounded-authority DERIVATION contract, which
  composes WITH their signing machinery.
- **SETTLEMENT — three ledger vocabularies** (voucher engine / RailLedger /
  watchpay), each sound; no shared settlement-outcome language for receipts.
- **RECEIPT — the strongest artifact family**: hash-chained voucher ledger
  + Rust conformance core + lexicon docket + three human views; but the
  accounting books are charge-side only — provider expense unbooked, tithe
  calculated+accrued but never SETTLED, no refund/credit event, chain_fee
  enum'd but unused.
- **RECONCILIATION — deep per-seam** (4-state audit, AV-3/5/6, HumanGate),
  but no closure predicate: nothing answers "did economic reality satisfy
  the committed obligation" as ONE verdict across the lifecycle.

**Collision check — GREEN for the claim, with two flags**:

1. ⚠ **VV naming overlap discovered**: zArcheology's
   `ADVERSARIAL-VENDING-SPECS.md` (on main @a0f8de3b) speccs VV-1..VV-6
   against the same vending contract while this seat's founder-ruled VV-1
   battery rides the unmerged `zcode/vv1-vending-pricing-2026-09-16`
   branch. Two honest parallel rolls, one ID space — the merge needs a
   naming reconciliation (flagged in the lifecycle doc; resolved by
   neither seat unilaterally).
2. The economic-artifact lifecycle LAYER itself is unowned — no doc, crate,
   or branch defines it; ADVERSARIAL-BPAY-SPECS/QUEUE govern the rail/
   authority domain, ASSURANCE-LEDGER governs assurance states; neither
   covers quote/invoice/receipt/reconciliation as ONE economic model.

**THE CLAIM (smallest non-colliding, GREEN under the concurrency
protocol)**: `docs/agents/BPAY-ECONOMIC-LIFECYCLE.md` — SPEC
BPAY-LIFECYCLE-1, the general economic artifact model: the lifecycle with
the founder's stage distinctions verbatim, the VV-1 boundary law reused
(frozen lane untouched), the accounting-separation law (nine books), the
two-representation law (friendly POS + cypherpunk evidence layers), the
reconciliation map of existing primitives (file:line, spot-verified on
current main), the collision fences, the zGenealogy consumer contract, and
the next smallest builds queued as bounded orders (INVOICE-1 the invoice
object; VOCAB-1 the settlement-outcome crosswalk; RECON-1 the closure
predicate). Pure documentation layer — zero production code, no shared-file
edits, own branch off current main.

**Producer/consumer contract recorded for the zGenealogy seat** (reciprocal
instruction banked): do not invent a genealogy-specific payment
architecture where generic bPay primitives exist or are being built —
consume them; own only the preservation-specific invoice/receipt content
and Autonomi/Trezor/retrieval evidence.

## Relay

```
bPAY ECONOMIC FRONTIER CLAIMED (zCode seat, founder-routed; WAIT held on
Gesture D + VV-1 GREEN). Collision-checked report done, claim landed GREEN:
docs/agents/BPAY-ECONOMIC-LIFECYCLE.md (SPEC BPAY-LIFECYCLE-1). Verdict:
QUOTE strong but fragmented (three TTL regimes); PRICING COMMITMENT exists
as fragments — the frozen VV-1 law IS the general principle, generalized
off-chain without touching the lane; INVOICE ABSENT (zero invoice structs
on main; only a decoder for other people's BOLT-11); AUTHORIZATION deep
but fenced (bpay-rail R20 owns signing; this lane owns the
invoice→bounded-authority derivation contract); SETTLEMENT has three sound
ledger vocabularies, no shared outcome language; RECEIPT is the strongest
family but charge-side only (provider expense unbooked, tithe never
settled, no refund event); RECONCILIATION deep per-seam, no closure
predicate. Accounting law: nine separate books, never collapsed. Two
flags: zArcheology's ADVERSARIAL-VENDING-SPECS VV-1..VV-6 overlaps this
seat's unmerged founder-ruled VV-1 — naming reconciliation needed at
merge; IF-1..IF-4 untouched per their standing note. Next queued bounded
builds: INVOICE-1, VOCAB-1, RECON-1. zGenealogy consumes; never invents
parallel payment architecture.
```
