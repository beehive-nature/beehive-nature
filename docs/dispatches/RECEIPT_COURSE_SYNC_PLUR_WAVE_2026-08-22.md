# COURSE_SYNC RECEIPT — the PLUR wave: museum showcase, timeline, kandi finish

**Seat:** zAgent (zCode session, GLM) · **Filed:** 2026-08-22
**Against:** `DISPATCH_ZCODE_PLUR_WAVE_2026-08-22.md` (landed 2026-08-21 as `c566c62`).
**Law swept before this filing:** the dispatch itself (governs), RAVER-REGISTER-LAW.md,
the corpus law in `surfaces/lang.js` (machine-draft ⚙ / visible fallback / consent never
finalized by machine; kandi NEVER-TRANSLATE lowercase including in caps headings), the
cache/version-chain law (corpus vN in lang.js + attest ×2, lang.js vN in tour.js, tour.js
vN on every page — count equals page total), the ≤3 Hz / WCAG 2.3.1 / reduced-motion /
pause-on-hidden animation law (plur.html Wing V says it; the page must not be a liar),
the pre-commit hex law (no 48+ hex runs), the D5 instrument grammar as embodied in
`bantfarm.html`, and §0.7 gate-open honesty (unbuilt looks unbuilt).

## §0 — Provenance

Research was **not** re-paid: every new claim in the rebuilt wing composes from
`docs/dispatches/payloads/PAYLOAD_IBIZA_SHOWCASE_2026-08-22.json` and
`PAYLOAD_SPAIN_CANNAHEMP_2026-08-22.json` (36 claims, 9 verdicts), which ride `c566c62`.
No new source fetches were performed by this seat for exhibit content. One deviating
detail is escalated in §2-E1 (buzz-studio's section), not resolved silently.

## §1 — Item-by-item against the dispatch

| # | Directive | Verdict |
|---|---|---|
| WI-1 | Rebuild `<section class="wib">` wholesale, showcase-first; new intro leads with "for much of the world, rave was born here" | **DONE.** New heading (`plur.wib` re-drafted ×27) + intro; card order: BIRTHPLACE → SPANISH DOOR (crown quote / legal hinge / uncovered floor) → MOLECULE ×3 → CANNABIS ×4 → Amnesia → Alfredo → 1987 pair → deep history (Sa Caleta → Bes → Tanit → Es Vedrà → UNESCO → Benjamin → 1973 → chillout → superclubs → state-vs-dance). Existing moved cards byte-identical, only `id=` anchors added. Exhibits 56 → 67. |
| WI-1 | Tierno Galván is the crown quote | **DONE, corrected form.** Verbatim incl. opening "Rockeros:"; Palacio de los Deportes 28–29 Jan 1984; El País 29-01-1984 + RTVE archive. "III Gran" edition number and Santo Tomás framing **dropped** per the verify verdict, with a ⚖ `.vn` line saying so. |
| WI-1 | Claims marked `corrected` ship corrected; `unsupported` do not ship | **DONE.** Corrections displayed as ⚖ lines: Tierno (edition number), therapy-era ("classics," not "main account"; 1978–85 bracket), ICEERS (constitutional attribution dropped, doctrine "recently revised"), Transform (Malta dropped — source names only Uruguay), Barcelona (50+ closures under Trias's 150 m zoning threat reversed by Colau — not "a third of 145"). **The La Movida "institutionalized from 1983" claim (verdict: unsupported) does not ship at all.** |
| WI-1 | Molecule card = HISTORY only; sassafras ancestry as family tree, zero methods | **DONE.** The ceiling is stated on the card's face ("No chemistry, no process, no dosage; that ceiling is this card's law"). Safrole named as encyclopedic ancestry + UN 1988 Table I listing; the founder's "sassafras tree bark oil" honored as documented family tree, explicitly not conflating tree with synthetic. |
| WI-1 | Cannabis: legal/cultural history, never guidance; hemp line linked to estate commons feed (h.117) | **DONE.** Four cards (legal ground / clubs / hemp / Ibiza-inside-the-model); hemp card links `dao-dashboard/` — the hemp-hearts commons price feed panel that carries corpus key h.117. Zero guidance anywhere. |
| WI-1 | Birthplace claim ships honestly (island = the rave WAVE; Chicago/Detroit hold the music in Wing II; existing 1987 record-vs-lore pairing stays) | **DONE.** Intro and BIRTHPLACE card both carry the wave/music distinction verbatim in spirit; the 1987 pairing moved up unaltered. |
| WI-2 | Horizontal scrollable era-ribbon, one node per era, comb cell or kandi bead, year + tiny label, click scrolls (`scrollIntoView` smooth unless reduced-motion) | **DONE.** 17 bead-on-a-string cells (654 BC Bes → 2026 this estate), hexagon beads in the ruled colors on a strand line; click → focus + smooth/auto scroll per `prefers-reduced-motion`; every target `id` verified to exist (gate below). |
| WI-2 | Per-wing inline SVG art in wing colors, extending the vocabulary | **DONE.** Seven medallions (train car · vinyl · statute scroll · kandi cuff · sensory tent · scales · Bes coin), stroke grammar matching the `.hs` handshake emblems, `aria-hidden`, static. |
| WI-2 | Perf/a11y laws binding | **HELD.** No new keyframe loops (existing comb 2.6–4.8 s; ≤3 Hz law intact); reduced-motion honored incl. ribbon hover transitions; decorative SVG `aria-hidden`; ribbon is `<button>`s (keyboard-focusable, focus-visible outline); text never color-alone (year + label text). |
| WI-2 | i18n: new HEADINGS need corpus keys ×26; years/names need none | **HELD.** Ribbon labels are years/names (unkeyed, per instruction); the one changed heading `plur.wib` re-drafted in all 27 renderings; no other new plur headings. textContent-only swaps, no innerHTML from corpus. |
| WI-3 | `kandi.bar` + `kandi.arms` ×26, kandi NEVER-TRANSLATE lowercase | **DONE.** Both keys ×27 (en + 26); enforced check: zero uppercase "KANDI" anywhere in the corpus; lowercase "kandi" present in every kandi.* rendering. |
| WI-3 | Doors: hub MAKE card; Wing IV handshake link line; build.html footer link | **DONE** (one deviation escalated §2-E1). Hub card sits in MAKE — see E1; Wing IV handshake exhibit carries "the ritual, live: 🍬 the kandi bar"; build.html footer link added. |
| WI-3 | The one cascade: corpus v8→v9 (lang.js + attest ×2), lang.js v9→v10 (tour.js), tour.js v18→v19 on ALL pages incl. kandi.html | **DONE, verified by count.** `grep tour.js?v=19` = 49 files (50 with festival, next commit), leftover v18 = 0; attest ×2 + lang.js fetch now `?v=9`; tour.js injects `lang.js?v=10`. |

## §2 — Escalations, by name, not resolved

**E1 — "hub MAKE section card … next to buzz-studio" is self-contradictory on today's hub.**
buzz-studio's card lives in the READ section (`surfaces/index.html` READ grid), not MAKE.
This seat followed the section-level instruction (MAKE, violet card, after the Hearth) and
left buzz-studio where it is. If Seat 3 wants kandi beside buzz-studio instead, it is a
one-card move.

**E2 — the gates were RED before this dispatch touched anything (repaired, flagged for
re-verify).** Baseline pasted in §5: footer said 47, live cards were 53, smoke expected 42,
review roster held 34 — three stale counts — and estate-review's harness served the tree at
root, making tour.js's absolute `/surfaces/…` injections (register.js, lang.js) 404 on every
crawled page; plus bantfarm's greymass read fails CORS from a localhost origin (the page
renders declared failure by design). Repairs, per the "assertions track the tree" law:
footer → 55 (53 + kandi + festival cards), smoke → 55 / roster 50 (roster extended with the
16 never-rostered surfaces incl. plur and kandi), estate-review gained a `/surfaces/` alias
(matching both real deployments) and a NET NOTES channel for `net::ERR_*`/CORS console
errors (local 404s and JS exceptions still fail). Seat 3 should re-run both gates on the
pushed Pages oracle.

**E3 — one ribbon node, two 1999 facts.** The suggested node "1999 UNESCO+Circoloco" names
two exhibits in different cards; the node targets the UNESCO card, and Circoloco's card
(which is also the 2016 "Space closes" target) sits two cards later. Splitting the node or
retargeting is a curator call.

**E4 — `plur.wib` corpus key re-drafted (27 renderings).** No attested tongue existed
(`_meta.attested` = {}), so no human signature was desynced — but re-drafting an existing
key is a corpus-history event: noted in `_meta.drafted`.

## §3 — Fences the work carried

- **F1 — quotes verbatim-from-payload only.** Every `.q` block in the new cards is a
  payload `quote` field, byte-faithful (El País opening "Rockeros:" retained; BOE art. 25.1
  and the Spanish hemp/Periódico quotes carry their accents).
- **F2 — the molecule ceiling is written into the artifact**, not just the process: the
  card states its own ceiling; no chemistry, process, dosage, or sourcing text exists in
  the wing.
- **F3 — unsupported ≠ corrected:** the La Movida institutionalization claim is absent,
  not softened into existence.
- **F4 — the cache law moved as one chain** (verified by grep count, §1 WI-3).

## §4 — Adjacencies, named so nothing is read as opened

- The museum's method footnote now records the showcase rebuild + payload provenance; no
  other mUseUm wing was touched beyond anchors and medallions.
- `RECEIPT_ANT_ETH_GAS_SPLIT_2026-08-21.patch` and `DISPATCH_SEAT3_HANDOFF_2026-08-21.md`
  sit untracked in the working tree from a peer session — untouched by this seat.
- Festival doors landed in the hub/tbar/roster with this commit's cascade (one-chain law);
  the room they open (`surfaces/festival/`) lands in the immediately following commit of
  the same push — no Pages deploy happens between them.

## §5 — Bus actions + gate receipts (pasted, real)

Baseline, before any edit of this dispatch (this session, 2026-08-22):

```
$ node e2e/estate-review.mjs
FAIL hub footer count matches card count — footer=47 surfaces cards=53
FAIL review roster is a sane superset of hub cards — roster=34 cards=53
(plus ERRORS storm: every crawled page 404ing /surfaces/register.js + /surfaces/lang.js)
1 passed, 4 failed

$ node e2e/university-smoke.mjs
FAIL hub counts 42
69 passed, 1 failed
```

After the work and the §2-E2 repairs:

```
$ node e2e/estate-review.mjs
PASS hub loads clean
hub: 55 cards, 54 local hrefs
PASS no broken intra-estate links
PASS hub footer count matches card count
PASS review roster is a sane superset of hub cards
5 passed, 0 failed        (exit 0; NET NOTES print the greymass CORS line, not counted)

$ node e2e/university-smoke.mjs
70 passed, 0 failed

$ node e2e/zcode-plur-festival-check.mjs      (this dispatch's behavioral pre-verify)
27 passed, 0 failed
```

Cascade count receipts:

```
$ grep -rl 'tour.js?v=19' surfaces --include="*.html" | wc -l   → 49 (+1 next commit)
$ grep -rl 'tour.js?v=18' surfaces --include="*.html" | wc -l   → 0
$ grep -c 'lang-corpus.json?v=9' surfaces/lang.js surfaces/attest.html → 1 + 2
$ node -e "…lang-corpus.json integrity…" → keys: 223 | non-27: 0 | caps KANDI: false
```

## §6 — Attestation

No `Signed-off-by` emitted (ORDERS-1 §3 DCO clause); credit via `Co-Authored-By`. Model
actually at this seat this sitting: **GLM (zai-coding-plan/GLM-5.3 builtin), the zAgent
seat.** Nothing pushed: per the landing protocol, Seat 3 verifies (browser + Pages oracle)
and pushes. Nothing designed beyond the dispatch; no escalation resolved.
