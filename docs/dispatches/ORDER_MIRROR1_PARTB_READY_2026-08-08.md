# ORDER — MIRROR-1 PART B: READY-TO-FIRE (2026-08-08)
**Authority:** Seat 0 (King Bee) — "ready" (connector authorized). Transcribed by research seat.
**To:** whichever seat first has LIVE GitHub write tools in-session.

## STATUS
Founder has authorized the GitHub connector. As of this writing the fork/create tools have **not** surfaced in either the research seat's session or Cowork's non-interactive session — connector auth does not propagate across sessions. **This order fires the moment a session has live GitHub write tools.** Do not attempt a blind org write without them.

## THE TASK (Part A is already done; only the fork remains)
Fork **`block/buzz`** into the **beehive-nature** org.

Part A receipt (already committed, `6b07aaf`) supplies everything else:
- Pin: **`02f640bc4559c48ac0c2ec595ef34dd2c294b0db`** (upstream `main` HEAD at verification)
- License: **Apache-2.0**, L-VERIFY done on the LICENSE file in the tree (not the sidebar)
- **No NOTICE file** → Apache §4(d) is N/A; the mirror carries LICENSE only
- All four named docs present at the pin (VISION.md, VISION_SOVEREIGN.md, VISION_MESH.md, AGENTS.md)

## TO CLOSE
Append **one line** to `RECEIPT_MIRROR1_STAGE1_2026-08-07.md`: the resulting mirror URL. Record the upstream SHA in the mirror's README per mirror-by-law. That closes Stage 1 entirely.

## FENCES (unchanged)
No clone or bulk download (read-only API for verification; the fork itself is the write). No blind org write. Stage 2's core-first mirror order stays untouched — it is post-blocker work and its `pay` verb is still gated by the two open custody blockers (archived `maidsafe/autonomi` external-signer survival; gas denominated in ETH against an agent earning ANT).

**Scope fence:** fork and record the URL. Nothing else. **Execute the prompt as written.**
