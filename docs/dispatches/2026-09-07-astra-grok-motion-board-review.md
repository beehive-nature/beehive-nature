# Grok campaign board — source review and motion integration handoff

Reviewed Grok's clean local campaign worktree and draft PR #28 at `b3fb7ed1`
after the founder shared the updated board. This is a source review of
`docs/mvp-walk/marketing.html`, the newcomer scorecard and festival brief.
Grok retains ownership of those files; Astra did not edit that worktree.

GitHub reports successful `tests` and `secret-scan` workflows for #28 at
`b3fb7ed1` and the motion package #31 at `3189280d`. Passing repository CI
does not establish the board's interaction or human usability behavior.

The browser URL policy refused the direct local file preview. No alternate
browser route or HTTP workaround was attempted. Therefore this receipt claims
source inspection, not a fresh visual or click-through check of Grok's board.
The previously recorded browser checks of Astra's studio remain separate.

## Confirmed progress

- The original-art section names LoVis and his mother, preserves the mixed
  gradients and distinguishes originals, Blender interpretations and provisional
  reconstructions. Purple means humans, teal AI and green biomass.
- skaists and LOVErnment DAO appear alongside the supplied mixed original and
  credited FUNGi, FROGGi and PEPi snapshots.
- The scorecard separates local handoff time from receiver Keep time and keeps
  declines after starting in the attempted-handoff denominator.
- The festival brief starts with recent events and "What happened?"; concept
  reactions are recorded separately. No human observations are manufactured.

## Next bounded Grok pass

Use the pushed #31 motion component at `3189280d` for the board. Its contract,
exports and assets live in `docs/mvp-walk/assets/genesis-3d/motion/README.md`.
Keep the shared shell and Blender builders in Astra's lane. Grok can consume
the vector, poster and optional film and shape their creative presentation.

1. **Every view keeps the controls.** At board lines 167–168, `.raver-only` is
   hidden for New bee and Cypherpunk; `setView` at lines 676–687 also stops the
   expression when leaving Raver. This makes functionality depend on view.
   Retain expression, intensity and pause while changing presentation, using
   the shared register control rather than a second independent view state.
2. **Pause holds the visible pose.** At line 163, removing the animation with
   `animation: none` resets the ring transform; resuming starts over. Reuse
   the SVG root's `.is-paused` behavior. Preserve zero intensity as a still
   choice; the current slider at line 320 starts at 0.4 and scales the overlay
   container instead of varying the bloom's movement.
3. **Every enabled button performs the promised action.** `#exprSound` at
   line 321 promises sound, but its handler at lines 709–711 only reports
   absence. Replace it with plain "Sound is not available in this preview."
   until a working audio choice exists. No audio production task is implied.
4. **Let the artist see the art first.** Move review-only implementation/status
   detail into a clear disclosure. Replace "because the founder asked for them"
   at line 296 with useful language about the people and project. Keep credit,
   color meaning and real capability limits easy to find.

Two provenance wording repairs belong in that same copy pass: line 231 should
say the Blender studies are "rendered together", not "photographed together";
line 384 should call the PLUR image an accepted concept still, matching the
later caption, instead of a "founder PLUR photograph".

Verification to return: all three views retain the chosen expression, intensity
and pause; pause/resume preserves phase; zero intensity and reduced motion are
still; no inactive sound action remains. Include narrow-layout inspection and
exact reviewed commit. Human feedback remains its own record.

This requires one medium-effort Cursor integration worker and Grok's own
integrator review. It does not need a fresh fleet. No cloud session was launched
by Astra; this docket is the founder-authorized repository handoff.

The existing lane split remains: #28 draft, #27 open, #31 draft; no public
campaign launch or production operation is part of this source review.
