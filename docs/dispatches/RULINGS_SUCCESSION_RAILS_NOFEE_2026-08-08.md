# RULINGS — SUCCESSION · .b RAILS · NO-FEE/CAP · UNTRACKED FILES · PUSH (2026-08-08)
**Authority:** Seat 0 (King Bee), transcribed by research seat to the mailbox.
**To:** all seats

## 1. SUCCESSION — bDiD IS FREELY ACCESSIBLE (closes the 1000-year hole)
The hierarchy ruling defined SUPERVISED as "approval by bDiD `lovis.b`" — one named human, which no scheme survives 1000 years. **Founder amends it:** the **bDiD is FREELY ACCESSIBLE**. SUPERVISED authority binds to the bDiD as an **accessible, successor-able role/credential**, NOT to the mortal individual. The approver is a role, not a person. Succession options were requested and this is the direction; the gap in the ruling is closed at the principle level. Spec work: define how the freely-accessible bDiD role is held, transferred, and bounded so "freely accessible" does not become "unauthenticated" — bring options, do not design.

## 2. `.b` FULL-FUNCTION RAILS = A + AR/ANT/b/etc under one bDiD
A `.b` identity's full function rails = a **Vaulta `A` (money) account PLUS Arweave + Autonomi + b + other adapter rails**, all under the one bDiD. This confirms the custody frame: the ANT/AR wallets are **rails of the identity, not foreign chains**, so agent spend is **flow execution under one identity (scoped delegation)**, never cross-chain atomicity. Directly seats the `approve_to_spend_tokens(finite)` + remote-signer custody path.

## 3. NO FEE ON `.b`; NAMESPACE BOUNDING = PER-bDiD NAME CAP
- **No registration fee.** All five jobs a fee could do fail (Code's analysis, founder accepted): cost recovery already met at zero fee; a fee can't create RAM; sybil belongs at the identity layer; spam handled by self-paid RAM; treasury revenue has nowhere lawful to land. WAX's 5-WAXP fee → 81–85% collapse, never recovered.
- **Bounding = per-bDiD name cap (option A).** Uses the uniqueness layer already being built; costs nothing, taxes nobody, scales. (Contested-name auction remains the honest anti-squatting shape if wanted later — not selected now.)

## 4. ROOT-LEVEL UNTRACKED FILES — COMMIT THEM (Cowork)
Founder ruled COMMIT the root-level untracked files (`ACTION_*`, `ASSESSMENT_*`, `RELAY_*`, `SOVEREIGN_*`, `bigen-pickup/`, `surfaces/`). Cowork: commit them with the same repo-is-the-record discipline (receipts, promote-don't-erase, no wholesale rewrite). Scope and land cleanly.

## 5. PUSH — see the separate confirm line
Push confirm rests on Code's public-repo argument (b-domain is already public; the held docs disclose nothing new), NOT on the freeze. Awaiting the founder's explicit one word before the flip.

**Scope fence:** these are rulings + directions; the succession mechanism, the cap parameters, and the rails custody are research + options, not seat design. **Execute the prompt as written.**
