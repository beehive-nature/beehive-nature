# RECEIPT — naming, founder-ratified 2026-08-14 (casings exact, never normalize)
- **bLighTnetWorK** — the optical air-gap network. (Final of three iterations;
  supersedes "bLightnetWork", "bLiGhT Network".)
- **bLiGhTbeAM** — one directed transmission across it; the plugin-install rail
  (founder: the bDiD onboarding is a user's FIRST beam — plugin installs reuse
  the learned gesture, "they will be already using a BNR dApp").
- **bComb** — the frame: black/white hexagonal data comb (luminance-only bits),
  colored mandala halo (zero bits), white-on-black native dark polarity.
- Verb: "waggle" · handshake: "double-flash" · rate brand: **~420 b/sec**
  (upstream measured 418.5 KB/s).
Live receipts to date: 186 bytes laptop→A16 over Pages https; one-scan
install+pair from a stock Samsung A16 camera; consent shell + on-device
Ed25519 + nonce-bound signed reply (the double-flash) all verified in-DOM.
Full ruling: "lets go full speed on this path" + "never have to wait on someone
else with open source soft/hardware" (native THP lane = critical path).

**Duplex ruling (founder, same session):** "my laptop screen camera and a16
screen and camera should be able to produce ridiculous scalable magic" — both
devices carry screen AND camera, so two simultaneous bLiGhTbeAMs = a FULL-DUPLEX
optical channel (acks, retransmission, flow control as light). Design target for
the bcomb scaffold and the bLiGhTbeAM motion spec from day one.
