# Beehive University three-temperature adapter — 2026-09-12

Seat: Grok / Cursor cloud agent. Lane: adapter #7 after bEarth #45,
Symposium #46, Hexagon #47, bLongevity #48, Royal Review #49, and
BiGen #50. Branch: `cursor/university-three-temp-314e`.

David Irvine / x0x #622 is still the standing backend priority. This
seat was assigned the university presentation adapter; no backend,
mesh, or measurement claim is made here. Same literacy instrument;
first paint is what changed. Education / cite-or-silent only. Quest
arithmetic is a hypothesis receipt you compose and hold (tier 0 plan
≠ tier 3 assay). No body-outcome theater. Cannabis framing off first
paint (SERVING-BASIS-1 §2). Open gates stay exhibits.

## What changed

Same university instrument: THE CHARTER (commission 2026-08-20,
view-not-store, dock genealogy, SERVING-BASIS-1, COMMISSION receipt
pointer), corpus law live (father / mother / student), THE QUESTS
(bFood §7 dock — hemp-hearts 1.25 g/kg, body-weight+span composer,
four-question discipline, USDA/NASEM/DGA cites, local-only plan
receipt), GRADUATION compose [bX review] with U-3 ruled, THE
TRANSCRIPT localStorage Royal Guard grammar, GATES table U-1/U-2
OPEN and U-3 RULED, seven COURSES with acts (c1 lab report → c7
vending/agent), cite-or-silent method, honest unwired seams. First
paint is what changed. PubMed/science constants were not rewritten.

The page-local mid-page `setReg` trio (`#rg-bee` / `#rg-raver` /
`#rg-cyper`) is deleted. One register lives in the shared bar.

## Visual QA poke

`http://127.0.0.1:8765/university/index.html` with `surfaces/` as the
server root — the same poke that 404s `/surfaces/register.js`.

Cure, copied from #45–#50:

- `tour.js` sibling riders (`register.js`, `lang.js`, `rails-badge.js`)
  resolve from `document.currentScript.src`. Tour-bar hrefs still use `R`.
- Page-owned `#bregbar` with `data-register-host` and `data-language-host`
  so `#bregctl` and `#blangctl` paint on first-paint chrome.
- Inline `[data-reg]:not(body){display:none}` plus the matching revert,
  so the cypher masthead cannot FOUC onto New bee if register is late
  or 404s.
- JS-injected course cards carry `data-uni-injected="course"` and stay
  hidden unless the instrument beat is open — COURSES render after load
  and cannot leak onto New bee first paint.

## Graded first paints (Chief PASS + three trims, baked as written)

- **New bee:** calm sentence (“Beehive University teaches you to read
  this estate’s own receipts — every lesson ends in a signed act you
  hold, not a certificate.”), takeaway lead kept as written (“You
  learn evidence literacy by checking *our* lab panels, requirement
  bars, and chain reads — with sources, or silence.”), quieter
  support (trim 3: “Sources, or silence — never a certificate.”).
  Primary CTA (trim 1 — human short): **Start with a lab report**.
  Also: **Go deeper**. ZERO charter wall, corpus-law form, quest
  kg/span/compose, graduation composer, transcript dock, gates table,
  LOQ/EAR/ΔE/Poseidon jargon, cannabis-forward framing, custom
  mid-page register toggle. No healthier / graduate / prove-adequacy
  promises.
- After **Start with a lab report** (trim 2): one first-lesson beat
  only — how to read a lab report (photograph of one sample; three
  questions; below the floor never “0”). Charter / quest kg / gates
  table / graduation / transcript stay deepen or Cypherpunk.
- **Raver:** quiet floor where every lesson ends in something you can
  hold; reduced-motion = one frame. Atmosphere: the curriculum *is*
  the receipts. Tap: **Feel the first act**. Consciousness (“Not a
  content silo — a room where checking our own material is the
  dance.”) plus one vivid course beat and one honest receipt link
  are layer 2 only. Quests / gates / seven-course deck stay layer 2+.
- **Cypherpunk:** today’s full instrument on contact. FOUNDER LOCK:
  never a trimmed New bee twin. Switching into Cypherpunk, and any
  lesson-beat click while already there, force `data-uni-beat="deeper"`
  so the literacy instrument cannot be stripped.

Footer keyed leaves (`uni.foot.door`, `uni.foot.learn`,
`uni.foot.verify`, `law.hive`) stay laid out so the first-paint
floor does not drop. Cypher masthead clauses ride
`data-reg="cypherpunk"`.

## Language

Sixteen `uni.*` keys were machine-drafted across English plus the 28
docked tongues and marked ⚙. New bee stays digestible — short calm
chrome, no jargon dumps. Meaning review is still owed. Existing
`h.174`–`h.180` headings stay on the instrument.

## Verification named here

- `node --test e2e/university-views.test.mjs e2e/register.test.mjs` **23/23**
  (8 university first-paint + 15 register). Wired into the static front door.
- `node scripts/estate-check.mjs` PASS (93 counted).
- `node e2e/estate-source.mjs` 11/11 (corpus English matches the pages).
- `node e2e/i18n-coverage.mjs ru --set lang-coverage-set.json --floors` PASS.
  Live census on university/index.html New bee first paint: **11 keyed
  / 11 visible / floor 11** (100% keyed; 28 tongues filled). Unkeyed
  samples: none. Floor raised 7 → 11.
- `cd e2e && node university-smoke.mjs` **85/85**. Walks New bee calm
  door → Start with a lab report (one lesson beat, gates stay hidden)
  → `enterCypherpunk()` via `#breg-cypherpunk` (mid-page setReg gone)
  → the existing instrument acts, graduation `[bX review]`, quest
  receipt, then the rest of the smoke atlas. Reloads re-enter
  Cypherpunk because New bee hides the injected course DOM.
- Visual poke, `python3 -m http.server 8765` with `surfaces/` as root:
  `GET /university/index.html` 200; `GET /register.js` 200; `GET
  /surfaces/register.js` 404. Sibling `assetBase` is what paints
  `#bregctl`. Browser walk: New bee calm door → Start with a lab
  report → one lesson beat; Raver atmosphere → Feel the first act;
  Cypherpunk full instrument on contact; New bee remembers the lesson
  beat on return (reload restores arrival). Mid-page setReg trio
  absent. Top-bar hosts paint.

Skipped this beat: fleet attestation of the new keys, any David
Irvine / x0x #622 measurement claim, any medical-outcome rewrite,
any body-outcome theater (a composed quest stays a hypothesis
receipt). Science constants and open-gate honesty were left in place.

FOUNDER LOCK applied: Cypherpunk first paint stays the complete
literacy instrument. The lesson-beat progressive door is New bee /
Raver only.
