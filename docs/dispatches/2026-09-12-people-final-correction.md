# People journey: final correction before integration

Astra read z1.b's pushed receipt `b6d91011` reviewing PR #57 at `f19513c1`.
Accept F2/F3 as independently reviewed fixes and keep F6 retracted. The
reviewer's test/render receipts are their evidence, not an Astra rerun.
Do not merge yet: inherited F1/F4/F5 still fall within the founder's requested
translation and mother-readable New bee scope.

## z1.a — existing implementation session, Medium effort

Work on the PR #57 branch, fetch first, preserve other seats' changes, and
publish a descendant. Fix only this bounded remainder:

1. Key the three mid-beat support sentences identified by z1.b (directory
   door cards and profile house), plus the unkeyed instrument section
   headings and law copy on these two pages. Populate supported corpus cells
   using the established machine-draft provenance; do not claim human review.
2. Set routine New bee labels to at least 14px: directory relay/chips and
   profile description/metadata. Scope overrides to New bee. Verify wrapping
   at 390px and RTL without hiding labels or shrinking them back down.
3. Replace the shot walk's vacuous profile language assertion with an actual
   expected translated profile string. Keep CI wiring out of this patch.

Add focused regression assertions for translation hooks, actual rendered
non-English text, computed New bee sizes and overflow. Run relevant page,
corpus/source and browser checks once on the completed patch. Return exact
head, test results and dispatch on #10. No shared chrome rewrite or deployment.

## z1.b — wait, then bounded verification

No full repeat audit now. Once the new candidate arrives, recheck F1/F4/F5
and the repaired profile assertion at that exact head; inspect mobile/RTL
wrap and confirm Cypherpunk styling remains unchanged. Return the residual
finding status. Astra retains integration.

The 390px Raver biomass crop is a recorded visual refinement, not part of
this blocking patch. Screenshot output-directory/CI wiring remains a separate
Astra decision. Avoid repeated narrative/tool polling while waiting; each
seat's next return should be a short receipt with commit, checks, and gaps.
