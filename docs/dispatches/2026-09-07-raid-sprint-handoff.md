# Raid and two-goal sprint handoff — 2026-09-07

Prepared by Codex from the founder's current chat and supplied documents. Repository
baseline: `df895c7c` fetched from origin/main. Work is in the isolated
`codex/raid-sprint-handoff-2026-09-07` branch. No sprint execution or mail delivery is
claimed by this preparation receipt.

## Raid result

[FACEPLUGIN-SORT-2026-09-07](../raids/FACEPLUGIN-SORT-2026-09-07.md) carries the founder's
**TAKE upstream detector only** ruling, source pins, four-axis review and corrections to
the attached face-uniqueness research. Faceplugin's package is not taken; its native
source/license gaps remain. Linzaer code has an MIT receipt; pretrained weights,
hardware fitness and the full recognition/private-dedup system are separate work.

The detector was not installed or integrated. No face was captured, no b entitlement
was issued, and no identity policy was changed. The research documents remain source
material, with their fingerprints recorded; embedded instructions were not executed.

## Two prepared orders

Intended recipient from the supplied conversation: **bclaude@agents.skaists.buzz**.

1. [Latvian voice on bMesh — recognition first](2026-09-07-order-latvian-voice-mesh.md).
   Continue from the existing voice-scribe implementation. Measure held-out WER and
   blind changed-meaning counts; add/prove mesh provisioning, per-audio-second metering,
   shared room defaults and explicit language refusal. Tongue order recovered from the
   repo: lv, th, ru, uk. Model artifacts must be eligible for their intended use; current
   MMS ASR and Latvian TTS cards have noncommercial terms.
2. [Zano HF6 adapter pass](2026-09-07-order-zano-hf6-adapter.md).
   A clearly labeled reconstruction from the existing audit, because the original
   second order was absent from the pasted conversation. Revalidate format deltas,
   independent prefix vectors and supported confidential spending; explicitly refuse
   unsupported gateway outputs. Preserve the frozen crypto choices.

The old Latvian receipt demonstrates one real transcription and UI checks, not the
new held-out semantic acceptance. No WER value, model winner, HF6 pass or runtime
resource measurement was invented in this handoff. The separately mentioned full
provisioning-law text was not located; the orders name this limitation.

## Continuity with the previous session

The funnel is already receipted in [RECEIPT-FUNNEL-BROADCAST-2026-09-05](RECEIPT-FUNNEL-BROADCAST-2026-09-05.md).
It records a Base Sepolia self-transaction carrying the split instruction. This is not
a receipt of actual mainnet fiat settlement or execution of the encoded seller/tithe
transfers. No second faucet request, address filing or broadcast was performed.

The supplied prior OmniVoice handoff references commit `9bcbeb3` and VERIFIED-FACTS
rows A90–A94. Neither that commit nor those rows was present in the inspected checkout.
This raid does not claim to recover them, recreate their bytes or reverify their facts.
The attached OmniVoice picture is preserved as context only.

## Verification of this preparation

Source trees were inspected through the GitHub API with `truncated=false`; selected
raw source and license files were downloaded into a temporary review cache and read
without execution. Linzaer and OpenCV model-directory license bytes were hashed.
This is a source/documentation review; no biometric or speech benchmark ran.

Checked locally: relative Markdown links resolve; new files end with a newline;
the existing ruling-ledger bytes remain an exact prefix; fact IDs are unique;
`git diff --check` passed. Commit publication also runs the repository secret and
identity hooks. Only the named documentation paths belong to this handoff. A pushed
branch establishes availability in the repository; it does not establish that the
recipient read or began the orders.

**Email status:** no mail connector was available in the callable tools. The available
browser opened Gmail to its signed-out Google Workspace landing page, not an inbox.
No email was sent, no draft was submitted, and no credentials were requested or entered.
The intended recipient and the two full orders are recorded above for later delivery.
