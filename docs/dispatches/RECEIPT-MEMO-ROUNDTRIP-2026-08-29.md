# RECEIPT — THE MEMO ROUND-TRIP: the A-rail binding flag CLOSED on jungle4 (2026-08-29)

**The flag:** "a balance delta can't see a memo — this is what closes it." Closed, end to end, real money movement on testnet:

| step | evidence |
|---|---|
| account | `bnrapolltest` created on jungle4 by `junglefaucet` (founder's captcha gesture, tx `6a800629…`), PowerUp'd (`ed065442…`), faucet-funded **100.0000 EOS + 100.0000 A** (`9a44ff18…`) |
| keys | owner/active/memo trio generated by the founder, WIFs **vaulted on the box at 600, never printed, never in the repo**; active key verified = the account's on-chain permission byte-for-byte |
| **the memo'd transfer** | **5.0000 A, bnrapolltest → kingbeelovis (jungle4), memo `apoll-proof-1`, tx `574ce0e5ecbe2ac3…`** — signed on the box (eosjs, the hand-rolled pure-python signer retired honestly after its checksum-law lessons: EOS pubkey checksum = ripemd160(pub)[:4] directly; core.vaulta forbids self-sends) |
| **chainpoll reads the ACTION** | transfer actions via greymass history on jungle4: from=bnrapolltest, memo, quantity, txid — the same per-transfer shape as basepoll |
| **memo routes the credit** | `+5.0000 A → key apoll-proof-1 (from bnrapolltest, memo-routed, tx 574ce0e5…)` — escrow event `c5668002…`. **The memo IS the binder; no binding table touched.** |
| balance visibly rises | the LIVE wallet surface (skaists.dev/surfaces/wallet.html) shows **apoll-proof-1: 0.0000 → 5.0000** — screenshots at 390px + 1280px in `e2e/shots-voucher/memo-bump-*.png` |
| charge + tithe on the memo-funded key | `total 0.6600 A incl. tithe 0.0600 A` — event `68d494db…`, balance 4.3400 |
| refuse-before-write | `148.5000 A exceeds 4.3400 — REFUSED, nothing written` |
| chain | **8 events green, tip `68d494db52736de0…`** |

**Test/mainnet seam (honest):** the jungle poll runs via env overrides (`CHAINPOLL_HOST/CONTRACT/ACCOUNT/STATE` — mainnet's own state file untouched); the escrow ledger is THE one ledger — the memo-funded credit, charge, and refusal above are real events in the live chain the voucher bridge serves. Mainnet memo routing uses the identical code path against `kingbeelovis` (watch_account) — armed the moment real A moves there.

**Signer note:** the proven path is eosjs on the box (`~/eosjs-sign/send.mjs`); the three dead ends that taught the checksum laws are retired with the record above.
