# COURSE_SYNC RECEIPT — the Festival Attendee experience (the eco's first dApp face)

**Seat:** zAgent (zCode session, GLM) · **Filed:** 2026-08-22
**Against:** `DISPATCH_ZCODE_FESTIVAL_ATTENDEE_2026-08-22.md` (landed as `c261370`).
**Law swept before this filing:** the dispatch itself + its parent PLUR-WAVE dispatch (the
shared cascade), the mUseUm grammar it inherits (comb, kandi beads, four PLUR colors,
RECORD/LORE honesty), the ≤3 Hz / WCAG 2.3.1 / reduced-motion / pause-on-hidden / no-rAF
animation law, the beam-perf lessons (pre-rendered glow, no shadowBlur-per-frame), the
corpus law incl. the kandi NEVER-TRANSLATE lowercase law (caps headings included), the
external-link law (tour.js delegation, inherited by riding tour.js), build.html's
multi-dApp doctrine (child seeds, never the root), §0.7 gate-open honesty, and the
D5 instrument grammar as embodied in `bantfarm.html` (method printed under every figure;
failure rendered as declared failure, never as 0).

## §1 — Item-by-item against the dispatch

| # | Directive | Verdict |
|---|---|---|
| 1 | The mUseUm stays a museum; attendee experience is a NEW surface inheriting its grammar | **DONE.** `surfaces/festival/index.html`; plur.html untouched by this commit. Grammar inherited: comb cells, bead ribbon, four PLUR colors, honesty badges (LIVE / DOOR / GATE). |
| 2 | Home: `surfaces/festival/` wing, index.html = Attendee; family gets stub doors, honest "gate open" cards per §0.7; no invented cased brand names | **DONE.** Four gates — the Festival Producer, the Dancer, the Sound artist, the Visual artist — the founder's plain words; each states what lands behind it (Producer: antd dock + treasuries · Dancer: wellness/Lane-C opt-in per the sandbox-study law · Sound: bset/studio lane · Visual: buzz-studio/bLighT lane); shared `f.gate` label ×27. |
| 3 | ARRIVE beat — tongue (26) + register 🐝/🎛/⚗ (lang.js corpus · register.js · keyless law) | **DONE.** Live: the picker and register switch ride tour.js; three-register prose throughout (raver = home voice, bee default, cypherpunk method); plus a stateless day-pass strand (name → 7 beads, nothing stored, FNV-hash seeded). |
| 4 | THE RIBBON — clickable comb-cell stage/moment map, plur timeline grammar animated up | **DONE.** 9-beat strand reusing the exact `.rib` grammar built for plur.html this same wave (the reuse the dispatch ordered); click → smooth/auto scroll + focus. |
| 5 | THE FLOOR — 127-comb as the light rig, beams, sparks; perf-lawful | **DONE.** Deterministic 127-cell comb (count asserted at render; a wrong count declares itself in the aria-label, build.html's own honesty pattern), 5 beams, 12 sparks; CSS keyframes only; cycles 2.6–4.8 s cells / 9–16 s beams / 26–48 s sparks — nothing near 3 Hz. |
| 6 | SPEAK LIGHT → blight/demo.html | **DONE.** Door card, method named. |
| 7 | KANDI → kandi.html (live) | **DONE.** Door card + codec summary; KND1 checksum-not-signature stated. |
| 8 | THE ECONOMY BOARD — live dials: gas, blocks; D5 grammar, keyless reads | **DONE.** Three dials (eth_gasPrice log-scaled 0.005–5 gwei · eth_blockNumber · eth_getBlockByNumber→gasUsed 0–30 Mgas), method printed under each, bantfarm's two-host failover list reused verbatim, 20 s cadence paused on hidden, raw lane line, declared-failure rendering ("never zero"). |
| 9 | THE SET → bset.html · listening.html | **DONE.** Two door cards. |
| 10 | THE CALM TENT — sensory-safe, stated and real | **DONE — a feature beat, not a fallback:** one button stills every animation on the page (persisted locally, restored on return, re-applied after tab-hidden); reduced-motion yields the full static experience; the ≤3 Hz law is stated on the page in three registers. |
| 11 | LEAVE A RECEIPT → review.html ([bX review]) · attest.html | **DONE.** Two door cards + grammar note. |
| 12 | High animation within the law | **HELD** (see §1-5, §1-10; no rAF anywhere; decorative layers aria-hidden; text never color-alone — beats carry text labels + tags). |
| 13 | i18n: new headings = corpus keys ×26; single cascade with PLUR-WAVE | **DONE.** 11 `f.*` keys + shared `law.hive`, all ×27, riding corpus v9 (the one bump — no second version churn); kandi lowercase law verified corpus-wide. |
| 14 | Registers: three-register prose start to finish; raver home voice, bee default never a downgrade | **DONE.** Every beat carries bee/raver/cypherpunk prose; register.js default (bee) untouched. |
| 15 | Honesty: page states demo vs live vs founder-gated | **DONE.** Per-beat tags (LIVE/DOOR/GATES) + a closing honesty ledger naming all three classes. |

## §2 — Escalations, by name, not resolved

**E1 — "the .b census" dial omitted from the economy board.** The dispatch's table lists
"live dials: gas, blocks, the .b census"; the .b census lives on the Vaulta rail
(`get_table_rows` on kingbeelovis/domains), and bantfarm's own Vaulta lane is the one read
this box's network/CORS posture flakiest on. This seat shipped gas/blocks/gasUsed on the
Arbitrum rail and pointed the full cockpit (bantfarm, both rails, failover) as the door.
Adding the census dial is a one-cell extension when Seat 3's oracle shows the Vaulta lane
clean from Pages.

**E2 — the day-pass strand is an addition, not an ordered beat.** "ARRIVE" ordered tongue +
register; the strand (stateless, no storage, deterministic) was composed under order 1's
"let the vision write the code." If it reads as scope creep, it deletes cleanly — it is
one `<div>` + one function.

## §3 — Fences the work carried

- **F1 — nothing mocked, everything a real door:** every beat wires an existing surface;
  the four family gates say "not built yet" in 27 tongues.
- **F2 — the animation law is stated AND measured:** frequencies listed in the cypherpunk
  register; the calm tent enforces stillness for the whole page, not just the rig.
- **F3 — the naming fence:** no cased brand names invented; the founder's plain words only.
- **F4 — the cascade fence:** festival rides tour.js?v=19 / corpus v9 — the chain bumped
  once, in the parent dispatch's commit, per the one-cascade ruling.

## §4 — Adjacencies, named so nothing is read as opened

- The tbar entry (🎪 festival/), hub VISIT card, and review-roster row for festival landed
  with the parent commit's cascade — same push, no intermediate deploy.
- The Founder's order names Producer/Dancer/Sound/Visual "for the festival eco … later":
  nothing in this commit pre-builds them; the gates are content, not scaffolding.
- b-indexer / Base Batches lanes untouched.

## §5 — Bus actions + gate receipts (pasted, real)

```
$ node e2e/zcode-plur-festival-check.mjs
PASS festival: no page errors
PASS festival: 9 beats render
PASS festival: ribbon cells reach their beats
PASS festival: 127-cell comb rendered
PASS festival: 4 family gates, honestly labeled
PASS festival: calm button stills the rig
PASS festival: calm toggles back
PASS festival: day pass strands beads
PASS festival: economy dials show value or declared failure
PASS festival: tour bar + language layer present
PASS festival: de heading docked (f.floor)
27 passed, 0 failed        (combined with the plur/kandi checks of the parent receipt)

$ node e2e/estate-review.mjs        → 5 passed, 0 failed (festival crawled via its hub card)
$ node e2e/university-smoke.mjs     → 70 passed, 0 failed
$ grep -rl 'tour.js?v=19' surfaces --include="*.html" | wc -l    → 50 (49 + festival)
```

Corpus integrity after the f.* additions:

```
$ node -e "…lang-corpus.json…" → keys: 223 | non-27: 0 | caps KANDI anywhere: false
```

## §6 — Attestation

No `Signed-off-by` emitted (ORDERS-1 §3 DCO clause); credit via `Co-Authored-By`. Model
actually at this seat this sitting: **GLM (zai-coding-plan/GLM-5.3 builtin), the zAgent
seat.** Nothing pushed: Seat 3 verifies (browser + Pages oracle) and pushes. No escalation
resolved; E1/E2 stand for Seat 3's call.
