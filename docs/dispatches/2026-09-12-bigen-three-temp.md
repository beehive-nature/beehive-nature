# BiGen three-temperature adapter — 2026-09-12

Seat: Grok / Cursor cloud agent. Lane: adapter #6 after bEarth #45,
Symposium #46, Hexagon #47, bLongevity #48, and Royal Review #49.
Branch: `cursor/bigen-three-temp-5aed`.

David Irvine / x0x #622 is still the standing backend priority. This
seat was assigned the BiGen presentation adapter; no backend, mesh,
or measurement claim is made here. Same evidence-library instrument;
first paint is what changed. Not medical advice. No pooled-estimate
theater. Claims arrive graded or not at all.

## What changed

Same evidence map: Cochrane contrast, three reading levels from one
constant set, exhibit 001 verdicts, gap map, COA undercount exhibit,
register, study integrity cards, Lundh 2017 pointed at ourselves first,
YAML fork, cite-or-silent sources. First paint is what changed.
PubMed sweep date, PMIDs, Lundh RRs, and the Hussein MEASURED/CLAIMED/NOTE
triple were not rewritten.

## Visual QA poke

`http://127.0.0.1:8765/bigen.html` with `surfaces/` as the server
root — the same poke that 404s `/surfaces/register.js`.

Cure, copied from #45 / #46 / #47 / #48 / #49:

- `tour.js` sibling riders (`register.js`, `lang.js`, `rails-badge.js`)
  resolve from `document.currentScript.src`. Tour-bar hrefs still use `R`.
- Page-owned `#bregbar` with `data-register-host` and `data-language-host`
  so `#bregctl` and `#blangctl` paint on first-paint chrome.
- Inline `[data-reg]:not(body){display:none}` plus the matching revert,
  so the cypher masthead (Cochrane tradition / YAML plots / exhibit 001
  cannabis & cancer wall) cannot FOUC onto New bee if register is late
  or 404s.

## Graded first paints (Chief PASS + two trims, implemented as written)

- **New bee:** calm sentence (“This is an evidence library you can
  re-run — not a brochure you have to trust.”), takeaway lead
  (“Empty cells are findings — not failures.”), quieter support
  (“exhibit 001 is early-phase and honestly not poolable — cannabis
  & cancer, named once.”). Cannabis & cancer is named once on this
  paint — not a disease-brochure wall. Choice: **Open the map**.
  **Go deeper**. ZERO Cochrane comparison, verdict grids, Lundh
  tables, study-card walls, YAML dump.
- After **Open the map:** one calm map beat — empty cells / not
  poolable. Cochrane table + Lundh RR + integrity cards + YAML do
  not share that beat.
- **Raver:** soft search / mark glow — library-as-garden, not a
  courtroom; reduced-motion = one frame. Feeling: “What if the empty
  cells were the most honest answer?” Tap: **Walk the map**.
  Consciousness (“A pooled number on an evidence map is a category
  error — this page refuses it.”) is layer 2 only, then the New bee
  takeaway stack.
- **Cypherpunk:** today’s full instrument on contact — Cochrane
  contrast, reading levels, evidence maps + verdicts, gap map, COA
  undercount exhibit, register, study integrity cards, Lundh
  instrument pointed at selves, YAML fork, cite-or-silent sources.
  Honest seams stay visible: not medical advice, not poolable,
  preclinical ≠ clinical. Sources use `data-view-disclosure` and
  default open in this view. FOUNDER LOCK (privacy-maximalist
  target): this view is never a trimmed New bee twin. Map-beat CSS
  is scoped `body:not([data-reg="cypherpunk"])`. Switching into
  Cypherpunk, and any Open-the-map click while already there,
  force `data-bigen-beat="deeper"` so the library instrument cannot
  be stripped.

Footer keyed leaves (`bigen.foot.library`, `bigen.foot.learn`,
`bigen.foot.verify`, `law.hive`) stay laid out so the first-paint
floor of 12 does not drop. Cypher masthead clauses ride
`data-reg="cypherpunk"`.

## Language

Fifteen `bigen.*` keys were machine-drafted across English plus the 28
docked tongues and marked ⚙. `evidence library` means a re-runnable
map, not a brochure. `empty cells` are findings. `poolable` means
fit to combine into one number — this page refuses that on an
evidence map. Meaning review is still owed. Existing `h.033`–`h.043`
headings stay on the instrument.

## Verification named here

- `node --test e2e/bigen-views.test.mjs` (wired into static front-door)
- `node --test e2e/register.test.mjs`
- `node scripts/estate-check.mjs`
- `node e2e/estate-source.mjs`
- `node e2e/i18n-coverage.mjs` on bigen.html ru --floors (floor 12)
- live 8765 poke of New bee / Raver / Cypherpunk first paints

Skipped this beat: fleet attestation of the new keys, any David
Irvine / x0x #622 measurement claim, any medical-advice rewrite
(the page is an evidence map, not a treatment instrument), any
new pooled estimate (the negative control still holds).

FOUNDER LOCK applied before marking the PR ready: Cypherpunk first
paint stays the complete evidence-library instrument. The map-beat
progressive door is New bee / Raver only.
