# LANE M — b-METER GENESIS (founder-ruled: "b meter with A as our reference genesis") + THE TITHE + THE TIER LADDER
**Date:** 2026-08-29. **Phased P1 → P2 → P3, run whole.** Source committed at `scripts/buzz-meter/` (meter.py + rate_set.json); deployed on the box at `/opt/buzz-meter/` under systemd `buzz-meter.service` (CPUQuota 20%/256M — the meter is a guest of guests).

## The two riders, as law (recorded verbatim in rate_set.json so every receipt carries them)
- **THE TITHE (founder law):** the Claude-agents API lane prices at PROVIDER COST BASIS + **10% TITHE** in A — a **distinct TITHE line on every receipt, never buried in the rate**. The tithe percentage changes by **founder word alone**; rate_set versions track cost basis only. **No rate options go to the founder — that ask is dead.** Local/qwen lane pricing stays a later, separate question.
- **THE TIER LADDER (founder law):** FREE tier = the box/qwen compute lane (guest keys, no charge, capped fairly — the Lane-H service caps are the fair cap); PAID tier = the Claude-agents lane (A-metered, cost basis + 10% tithe). **Same meter, same receipts on both — free-tier receipts simply charge zero, so a guest's history is ready the day they step up.**

## P1 — RECEIPTS: LANDED (no money touched)
- **The meter** reads the EXISTING `/opt/buzz-compute/logs/usage.log` (llama-server print_timing groups), one receipt per completed generation, and emits `SpendReceipt` JSON per **docs/SPEC-SPEND-RECEIPT-1.md exactly**: unit **"A"** (A-first MVP), line items as EXACT integer token quantities, explicit `rate` + versioned `rate_set_ref` (rateset-v1), `total` **computed from line items at every serialization and never stored independently** (stored as `total_computed`, re-derived on validation), `visibility: private` with 600-permission append-only storage (visibility = access-control property of storage, per §3a).
- **Resource-class ruling recorded:** the spec's closed enum gained `prefill_token` and `decode_token` **by this lane's founder dispatch** ("line items = token quantities as resource classes (prefill/decode tokens)") — the only lawful way the enum grows; the meter enforces the closed enum and rejects unknowns.
- **The tithe line** rides every receipt (`tithe: {percent: 10, law: …}` on a distinct zero-quantity line) — on the free tier it charges 0 (10% of zero), and the LAW TEXT is on the receipt so the shape is byte-identical the day the paid lane lands.
- **Spec §7 acceptance:** enforced at emit-time in `validate()` — computed totals, integer quantities, closed enums, no fiat field anywhere, canonical-JSON round-trip byte-identity; `--selftest` runs the checks green on the box.
- **`receipt_id`** = sha256 over the canonical serialization with the id field omitted — the spec's §5 named candidate, flagged here as unruled; swap is a one-line change when the ruling lands.
- **Live proof (2026-08-28 23:59 UTC):** a real generation → receipt `receipt-a794404f…` emitted within one 5s poll: lines `(prefill_token 1, decode_token 3, tithe 0)`, total `0.0 A`, wall-time stamp correct. Backfill emitted 18 receipts for today's earlier usage. One calibration receipt (`receipt-2af339b2…`) carries a defective `occurred_at` from the first elapsed-conversion bug — kept on disk, append-only, noted here; the fix stamps live receipts at parse time (drift ≤ poll interval).
- **The founder's kind-44200 archive remains the client-side twin** (bMeshLLM ruling unchanged — this sidecar is the server-side ledger only).

## P2 — A-CREDITS (next): estate Vaulta account receives A top-ups; the meter credits balances by chain READ-BACK (confirmed reads only); per-key balances gate /compute; voucher/escrow seam per the alloy ruling — the meter emits receipts and settlement INSTRUCTIONS only, the baton fence holds (no payment processing, no custody of member keys).

## P3 — THE CLAUDE LANE (awaits founder-funded API credits): the Anthropic-API relay joins BEHIND THE SAME METER, same keys, same receipts at cost basis + tithe (rate_set v1 carries the empty cost-basis frame, versioned there when it lands). Eval access = free test lane meanwhile.
