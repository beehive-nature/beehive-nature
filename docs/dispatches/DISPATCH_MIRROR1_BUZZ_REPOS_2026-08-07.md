# DISPATCH — MIRROR-1: BUZZ REPOS IN + THIN MIRROR AGENT (2026-08-07)
**Authority:** Seat 0 GO (founder quoted the sequence back as the plan).
**Sequence (ruled):** Buzz repos in via GitHub → mirror agent specced THIN (fetch, chunk, pay, round-trip verify) → CORE REPOS FIRST, then the family grows.
**Runs alongside bug 1 — touches NO tokenomics constant.**

## STAGE 1 — REPOS IN (Cowork lane: repo/document ops)
Bring the Buzz repos into BNR-controlled GitHub via the plugged-in GitHub integration:
- Source: block/buzz + the VISION docs already ordered (VISION.md, VISION_SOVEREIGN.md, VISION_MESH.md, AGENTS.md).
- Fork/mirror into the beehive-nature org, PINNED at a specific upstream commit (mirror-by-law: record the upstream SHA in the mirror's README).
- L-VERIFY applies: verify the LICENSE file in the tree, not the sidebar.
- Receipt: repo URL + pinned upstream SHA + license finding, pasted.

## STAGE 2 — THIN MIRROR AGENT SPEC (Seat 1 spec, Code builds post-scope)
One agent, four verbs, nothing else: **fetch → chunk → pay → verify.**
- Fetch: clone at pinned SHA (the mirror is a snapshot, not a tracking fork).
- Chunk + upload: Autonomi (chunked) AND Arweave (bundle) — both legs of the eternal-data sandwich.
- Verify: round-trip per the raid ledger's own fidelity discipline — re-fetch from the rail, sha256 against source; a mirror is only claimed when hashes match. No claim without the receipt.
- **Payment, staged honestly:** Stage-2a pays the rails in their native tokens (ANT / AR) with costs receipted. The **b-metered wrapper is Stage-2b** — it lands only after the payment layer exists post-bug-1. The autonomous-b-spend ruling is the DESIGN TARGET; do not claim b settlement before the rail exists.

## ORDER OF MIRRORING (ruled: core first)
1. beehive-nature/beehive-nature (the kernel — the code that defines the eternal computer gets eternal storage first)
2. beehive-nature/bnri-cosmic
3. LOVErnment-DAO (specs = law; law gets permanence)
4. The Buzz mirrors from Stage 1
5. Then the family grows (adapter-chain mirrors per BNRoSe-3).

## FENCES
- This is BNRoSe-3's first live execution — its two-part-sandwich limit applies verbatim (eternal store = history; live tip stays on git).
- bLOVErAi durable path stays OFF all of this per standing fence.
- Receipt rule on every stage. Cross-repo commit references carry REPO TAGS.
- No tokenomics constant is coded here. **Anything beyond the four verbs is out of scope. Execute the prompt as written.**
