# x0x on the box — license verified, capped unit landed, mesh BLOCKED by the box's own egress wall (lane x0x, 2026-09-05)

Order: L-VERIFY license at HEAD (SKILL says MIT OR Apache-2.0) · install pre-built
arm64 (option A, review before run) · capped systemd unit + own identity dir ·
health + agent · join a public group + GUI via session token only · connect box
agent + laptop agent, prove ONE tailnet forward · remote exec stays DISABLED ·
mobile: no · data usage over 10 minutes.

## LANDED

- **L-VERIFY PASS (with one flag).** saorsa-labs/x0x at HEAD/v0.41.2: Cargo.toml
  `license = "MIT OR Apache-2.0"`; `LICENSE-MIT` verbatim full MIT (1068 B);
  README "MIT OR Apache-2.0". Flag: `LICENSE-APACHE` is a 763 B Apache-2.0
  NOTICE-form file, not the full license text (why GitHub's API reports
  NOASSERTION). Adoption rides the verbatim MIT leg.
- **Box install (option A, reviewed before run).** v0.41.2 linux-arm64 tarball:
  sha256 verified against the release asset (`a95d7465…f6c226f0eb`
  PUBLIC-CONSTANT), GPG good signature David Irvine (code signing), key CEB3
  506E 7DCB 8A2D D2D6 79E8 EDDA 4827 D89C 0F29 — TOFU against the
  release-shipped key (same-release bound, stated honestly). Tarball holds only
  `x0xd` + `x0x` + licenses + README. System user `x0x`, binaries in
  `/usr/local/bin`, config `/etc/x0x/x0xd.toml`.
- **Capped hardened unit.** `x0x.service`: MemoryMax 512M · CPUQuota 100% ·
  TasksMax 64 · IPAccounting · ProtectSystem=strict · empty capability set ·
  `ExecStartPre=x0xd --check`. Own identity dir `/var/lib/x0x/identity` (keys
  never under `~/.x0x`), data `/var/lib/x0x/data`. Daemon self-update OFF.
  In-tree verbatim: `ops/x0x/` (unit + config + this runbook).
- **Health + agent (box).** `ok:true, v0.41.2, healthy`; agent
  `1ca00a42…8df66367` ("boeing above destroy limb"), display `hive-box`.
- **Laptop agents + PUBLIC GROUP JOINED.** Windows binaries at
  `C:\Users\travi\x0x-win\`; default instance `hive-laptop`
  (`be9ad64a…5a656cb89`, 25 peers send-ready) created public group
  **hive-porch** (preset public_open: open_join · signed_public ·
  public_directory · public read); named instance `second` = `hive-worker`
  (`97b09c05…f20cddb00`) joined via member-minted invite
  (pending_authority_commit → seated, member_count 2, both active) and posted
  the seat-proof message, read back by the admin with correct attribution,
  fan_out 6. Join law found live: **no inviteless join exists in v0.41.2** —
  their own GUI joins by pasting `x0x://invite/…`; cards carry no join
  capability; the live public groups have requests disabled.
- **GUI shown, session token only.** `POST /auth/session` → 10-minute session
  token → `/gui?token=…` (durable token is never accepted in a URL). Live
  receipt `e2e/shots-x0x/gui-laptop-one-char-fix.png` +
  `gui-laptop-public-group-chat.png` (hive-porch chat + message + live peers).
  Box GUI receipted over an SSH tunnel with the box's own session token
  (`gui-box-ssh-tunnel.png` — labeled SSH, NOT tailnet).
- **Remote exec DISABLED both sides** (`/etc/x0x/exec-acl.toml` absent →
  `enabled:false, acl_missing` on box `--check` and laptop diagnostics; a live
  exec attempt refused before execution). **Mobile: no** — nothing built.

## THE WALL (the finding of the lane)

The box cannot reach the x0x mesh: **UDP egress is port-allowlisted above the
OS** (iptables OUTPUT policy ACCEPT). Probes from the box: DNS 1.1.1.1:53 →
reply; NTP :123 → reply; STUN 74.125.140.127:19302 → silent timeout; UDP
1.1.1.1:5483 → silent timeout. x0x is QUIC/UDP-only: all 24 embedded bootstrap
dials (12 hosts × 443/5483, v4+v6) time out (journal receipted), daemon healthy
but isolated at peers: 0. Hole-punching dies too (the box's outbound high-port
UDP leg is dropped). The laptop (unfiltered network) joined fine — the mesh
itself works. **Founder gesture to unblock: OCI security-list/NSG egress UDP
443 + 5483** (optionally INPUT 5483/udp). Then, unchanged: box joins the mesh,
laptop mints a hive-porch invite for the box agent, and the banked one-liner
lands the tailnet forward:

```
x0x forward add --local 127.0.0.1:18080 --peer <box-agent-hex> --target-port 12700
```

→ laptop 127.0.0.1:18080 = box loopback REST/GUI, end-to-end over post-quantum
QUIC. NOT proven today — blocked by the wall, not by x0x.

## NUMBERS (10-minute window, box unit, isolated = bootstrap-retry churn)

T0 00:32:17Z → T1 00:43:04Z (10 m 47 s, counters continuous — no unit restart,
NRestarts stayed 1; the daemon self-reports the isolation honestly:
`degraded_reason: zero peers for the whole uptime window (>120s); transport
may be wedged or the network unreachable`):
**ingress 845,336 → 906,560 B (+61,224 B / 582 pkts) · egress 2,257,436 →
2,318,900 B (+61,464 B / 582 pkts) ≈ 11 kB/min each way — pure 24-dial
bootstrap timeout retries.** Laptop side for contrast: ~35 min on the public
mesh as a Leaf moved ~1.54 GB eager gossip outbound (97.7k msgs; +118 MB ihave
+75 MB iwant +5 MB anti-entropy per `diagnostics gossip`) — the public plane's
relay burden is real; consider a trimmed `bootstrap_peers` for the box when it
joins. Box-side numbers re-baseline after the egress gesture.

## FLAGS

1. **Egress wall** (above) — the only blocker to the box seat + tailnet forward.
2. **Upstream GUI defect at v0.41.2 AND HEAD:** the served `/gui` closes the
   `addEventListener('peer-lifecycle',…)` callback with `};` instead of `});`
   — one-character parse error that kills the whole ~4970-line script in every
   browser (GUI = shell only, "Connecting… 0 peers", 1 pageerror). Introduced
   by `bdd3f46a` ("v0.41.1 — GUI event-stream repair"). Local one-char-fix
   receipt harness: `e2e/x0x-gui-proxy.mjs` (asserted single substitution;
   with it: 0 pageerrors, live spaces/chat/peers). Not yet reported upstream —
   founder word.
3. **LICENSE-APACHE is notice-form** (above) — MIT leg is the safe reliance.
4. GPG verify is TOFU against the release-shipped key; no out-of-band
   fingerprint channel yet.
5. `/diagnostics exec` on Windows prints the POSIX ACL path — cosmetic.

## NEXT

- Founder: open OCI egress UDP 443 + 5483 → re-run this lane's tailnet step
  (one command banked above) + invite the box agent into hive-porch.
- Report the GUI one-char bug upstream (patch is one character; cite bdd3f46a).
- LICENSE-APACHE full-text question upstream (low stakes while MIT rides).
- Then: hive-porch becomes the estate's agent porch — buzz-visible receipts of
  agent-to-agent cooperation can mirror there.
