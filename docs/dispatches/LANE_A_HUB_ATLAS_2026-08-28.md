# LANE A — HUB ATLAS REBUILD (sprint 2026-08-28)
**Seat:** zA (chief-run). **Plan:** docs/SPRINT-2026-08-28-PLAN.md lane A. **Law:** continuous landing — each item DONE at its receipt, then the next.

## Lane state
- **A1 — DONE @924fb1d (commit on origin/main + CI green run 33182903841, static job carries the new `Estate registry — counts computed, not written` step).** `estate.json` = the single source: 87 rows listed, 78 counted (census law below), 26 domains with front-repo truth, every count recomputed by `scripts/estate-check.mjs` in CI.
- **A2 — DONE @8ed8054 (live URL receipt: skaists.dev/surfaces/index.html, tests 33183739108-failure → fixed → success; Pages deployed; before/after at 390px in `e2e/shots-lane-a/`).** The hub IS the atlas now: static-rendered from the registry (`scripts/build-atlas.mjs`), one accent, family sections replace doors-row+panels+widget, search owns its space (0 overlaps verified), first fold speaks human.
  - **The red that taught the census law (kept as record):** first A2 push failed university-smoke 3 ways — (1) footer said 85, tree says 78: the founder-art fleet copies (`fleet-hosted/{lab,gallery}`) are **listed but honoured uncounted** ("fixing a bug in a copy does not transfer authorship"); (2) the fleet card sentence "N of them carrying behaviour fixes" is load-bearing raw text — now COMPUTED in the generator from the tree diff (9 today); (3) the reachability crawl walks RAW hrefs — JS-rendered links are invisible, so the atlas renders statically and search is the only javascript. Also: `orbit-v2` is counted by the tree but **never presented** (its own exemption's reason: presenting invites edits to the frozen original).
- **A3 — DONE (this file, committed).** Seven seats defaulted below.
- **A4 — DONE.** plur-earth repo live (see below).

## A3 — THE SEVEN ATLAS SEATS (defaults picked, logged, reversible by one registry row)
1. **studio-gate / studio-music → skaists.dev.** They are instruments today; they graduate to skaists.social when the social app ships.
2. **review / dao-dashboard → skaists.dev.** Internal proof tools; a future DAO home is a graduation, not a move.
3. **market → skaists.store.** Commerce is commerce; the store domain gets its first real seat.
4. **museum → skaists.art.** It is the collection's home; skaists.lol keeps lore-text pieces.
5. **Social: ONE engine, TWO skins.** skaists.social = the makers' skin, bnature.social = the organism/community skin — shared engine (atproto/buzz mirror lane), separate identities. No third overlap.
6. **Manifesto 01–04 → beehivenature.com** (full text behind the beehivenature door; the hub carries one human line — applied in the atlas footer).
7. **Missing fronts scaffold on graduation only** — the six repo-less fronts (skaists.social, bnature.social, beehivenature.com, beehivebiomass.com, beehivebuds.com, plur-earth) wait for their surfaces. **Exception executed as ordered: plur-earth (A4).**

*Method note: family assignments in the registry default to the panel a surface sat in on the live hub (the founder's own live grouping), overridden only by the atlas rulings above. A redline is one registry row + one `node scripts/build-atlas.mjs`.*

## A4 — plur-earth front
Repo `skaists/plur-earth` created public, holding page committed, Pages enabled from main. **DONE receipt: https://skaists.github.io/plur-earth/ serving.** DNS pointing stays founder-hands (known state). Registry updated: plur.earth → DNS-PENDING.

## Park note
Lane A parked complete. All four items landed live. Awaiting next lane call.
