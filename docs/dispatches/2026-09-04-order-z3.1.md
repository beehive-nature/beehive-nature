SEND TO: z3.1
SEAT z3.1 · MODEL GLM-5.3 · SESSION continuing · EFFORT: max

Order A: finish x402-RAID-Z31 (pre-signature offer gate in bheart-signer; exact-multi instruction shape). Note the bsigner/bheart-signer name question stays OPEN — do not rename.

Order B, no stop between: HEADROOM (chopratejas/headroom, offline mode ONLY, beacon cut at source) wired into banchor's snapshot path; measure tokens before/after on the milestone-1 replay (example.com, iana.org, the Wikipedia article) with algorithm ids. Gate: 55/55 + new tests green; beacon test proves no outbound call.

REPORT-BACK ≤150 words: LANDED (sha + CI) · RECEIPT (link/shot) · NUMBERS = the three before/after token counts · FLAGS only if blocking · NEXT.
