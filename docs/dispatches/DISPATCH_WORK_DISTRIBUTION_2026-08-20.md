# DISPATCH — the work distribution (acting chief, 2026-08-20 night → Friday 0700 Denver)

**From:** zAgent (GLM 5.3), acting chief by founder word · one file, four destinations.

---

## 0 · zAgent HOLDS (acting chief duties + own lanes)

1. **The chair's operating duties** (DISPATCH_ACTING_CHIEF): CI streak, push discipline,
   bus dedupe, verification gate. The estate stays green as things land.
2. **Ballot A / G2 status check — FIRST ITEM, Friday morning:** the evening rulings
   (`1f54cea`) ratified T-1…T-8 with the Ballot A/B boundary drawn. If Ballot A carried
   the rename, **the G2 wire-label rename (BDID-v1→BZDID-v1, bdidrec→bzdidrec, pinned
   vector regenerated, 26+6 suites re-run) executes on this seat** — pre-scoped since
   2026-08-19. Verify the ratified text before executing; never assume.
3. **The contamination-void wiring** (CV-1…CV-3): `material_integrity` + the
   `VOIDED_CONTAMINATED` verdict into the BiGen integrity schema, struck-never-deleted
   rendering — the law is mine; the wiring lands with receipts for the chief's Friday
   blessing.
4. **The consent-station surface** (SPEC-POPUP-IRB's named next build): offline-first,
   hash-pinned, CONSENT-1 grammar, three language roles, machine-draft-never-finalizes.
5. **University course 6 — "how to feed a species"** + the canine quest line (PS-4):
   the dials, the arithmetic, the exclusion ruling, the safety walls, the two-species
   ledger — teaching SPEC-PATH2SCALE-1.

## 1 · TO CLAUDE DESIGN — queued (token max till Friday; artboards after)

Two orders already on the bus, one addendum joining them:

- **ORDER_CLAUDE_DESIGN_D5** — the three register templates (bee/raver/cypherpunk)
  against the live university + bFood, plus the owed six-checks on university/biq.
- **ORDER_CLAUDE_DESIGN_ESTATE** — the gold-standard estate pass: D1 tokens to gold,
  the hub as flagship, bFood as the worked surface, honest motion, non-value states
  (absent · n/m · single-source · floor) as first-class design objects.
- **NEW — the animation addendum (this dispatch, §1a):** the founder's doctrine, made
  this night — *"I want to see as much graphic animation since we have the tech to
  really spread our talent."* It binds both orders: the motion layer is not garnish, it
  is the medium — and `surfaces/blongevity.html` (the fat education) joins the
  worked-surface candidates: its six-stage assembly line is CSS-basic tonight and is
  the design seat's first cinematic upgrade target. The D- grade it earned as a
  protein-first page and the A it is being graded toward are both on the record; the
  design seat inherits the standard: **animation serves the receipted story, never
  decorates past it.**

## 2 · TO zCode (fresh session, high-token carry) — the b-indexer build

**Implement SPEC-BINDEXER-0** (`docs/specs/SPEC-BINDEXER-0.md`): Rust + SQLite
read-only indexer, exSat (7200) first — the blockbook acceptance matrix is written into
the spec, the `sendtx` row is permanently NO, the two-oracle `sources` field is schema
law, status-confession rows included. Add the CI lane. A stranger-auditable single-file
database is the deliverable; nothing keyless-less ships.

## 3 · TO zbCode (fresh session) — the single unblock: ERC-20i §10

**Resolve SPEC-ERC20I-MECHANICS-1 §10: frozen seed vs recompute-from-holder** — your
named Friday lane from the work order, still the one measurement that unblocks the most
downstream work (the bRoSe OFFER's baskets, the Apiary tier-2 lane, the census's
induction economics). The method is the census's own: deployed bytecode, two independent
RPCs, receipts or a receipted UNVERIFIABLE. This is also the gate SPEC-PATH2SCALE-1's
cannabinoid-API dock eventually leans on — the earlier it closes, the earlier everything
downstream of it opens.

## 4 · FRIDAY 0700 DENVER — the chief's return ledger

Blessing pass on the acting-chief verifications (CI, museum dead-network) · the
contamination-void wiring · the consent station · course 6 · Claude Design's artboards
· the amino/EAA lanes he holds · and the G2 execution if §0.2 arms it.

**zAgent (GLM 5.3), acting chief, 2026-08-20.** 🐝
