# DISPATCH — Seat 3 → zCode (LEAD) · CLEAR THE DECK · 2026-08-21

**Founder word (2026-08-21):** *"lets push out everything that is going forward. have zcode
clear the deck."* Holds are lifted. Land the held threads, then work the forward queue
session-by-session (cadence law still governs *sequencing* — one phase per session — the
founder's word governs *authorization*, and it's now GO on all of it).

## NOW — land the held threads

1. **Thread B — Thread B hold LIFTED. Land the ANT/ETH receipt.** Commit `5771781`
   (`docs/dispatches/RECEIPT_zCode_ANT_ETH_GAS_SPLIT_2026-08-21.md`) on branch
   `zcode/ant-eth-split`. **Seat 3 already gate-verified:** applies clean, one file, +103/−0,
   scope fence held, arithmetic re-checked. It was cut against old main (`070b142`); main is
   now `a774d57`, so **pull-before-am / rebase** the single file onto current main and push.
   Conflict-free by construction (unique new path).

2. **Thread C — doc-correction pass, founder word now given.** One scoped patch:
   - `docs/storage-substrate-split.md` — $0.25/batch → $0.040; $0.000122/chunk → ~$0.0035;
     batch width "undocumented" → **256**; §3 "$39M gas/yr" → **~$158K** (recompute the
     identity, don't just swap the total). Cite the receipt (`5771781`) as the source.
   - `surfaces/bantfarm.html:117` — the "a little ETH for gas" line: bound it honestly
     (correct at archive scale ~4%, wrong under ~50 MB where gas is 38–49%). Line 126 stays.
   - **False-signal fix (k001):** the "64" that reads as batch *width* anywhere in-tree is
     the Merkle *threshold*; width is 256. Delete/correct, don't patch-over.

## FORWARD QUEUE — proceed, one phase per session, in this order

1. **Integration proof** — discharge BF-1's own "not DAW-tested" caveat: `clap-validator`
   and/or a live DAW load of the exact `BuzzGain.clap` artifact. Receipt as always.
2. **BF-2 visual starters** — platform Canvas/SVG (ruled), seed→renderer hex-art idiom.
3. **Yjs multiplayer** — shared-doc wiring over the huddle data channel.
4. **The guidance/learning lane** (§7) — the in-band explain + practice loops.
5. **VST3 opt-in** — behind the PF-1 GPLv3 source-on-request notice, only when reached.

## FENCES (unchanged)

Receipt rule (pasted command + unedited output, no ✅ without it) · ⚙ on AI-authored work ·
no freqlab code · PUBLIC repo hex-scan · check-before-acting. **The Discord posts (the
Autonomi ecosystem intro + the ANT/ETH reply) are the FOUNDER's to send — not a machine
seat, per the attestation law.** zCode self-lands the repo work per standing push auth;
Seat 3 available as gate on request, conserving founder usage.

**Seat 3 (Fable 5), 2026-08-21 — deck cleared for the burn. 🐝**
