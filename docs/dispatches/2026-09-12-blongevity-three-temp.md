# bLongevity three-temperature adapter — 2026-09-12

Seat: Grok / Cursor cloud agent. Lane: adapter #4 after bEarth #45,
Symposium #46, and Hexagon #47 (the live order on this beat:
bEarth → Symposium → Hexagon → WELLness / the fat education).
Branch: `cursor/blongevity-three-temp-934b`.

David Irvine / x0x #622 is still the standing backend priority. This
seat was assigned the bLongevity presentation adapter; no backend, mesh,
or measurement claim is made here. No medical advice. No health-outcome
rewrite.

## What changed

Same fat-education instrument: walls, six-stage assembly line, reverse
hypothesis with grades, weight/sex mirror, essential vs not, DIAAS
attention hook, evidence ledger, PMIDs, cite-or-silent. First paint is
what changed. Graded hypothesis (reversal / upstream medicine), ACiD
unmaking, disease/outcome claims, and dose stay on Cypherpunk contact
and on deepen only. The interpretation lane stays fenced.

## Visual QA poke

`http://127.0.0.1:8765/blongevity.html` with `surfaces/` as the server
root — the same poke that 404s `/surfaces/register.js`.

Cure, copied from #45 / #46 / #47:

- `tour.js` sibling riders (`register.js`, `lang.js`, `rails-badge.js`)
  resolve from `document.currentScript.src`. Tour-bar hrefs still use `R`.
- Page-owned `#bregbar` with `data-register-host` and `data-language-host`
  so `#bregctl` and `#blangctl` paint on first-paint chrome.
- Inline `[data-reg]:not(body){display:none}` plus the matching revert,
  so the cypher masthead (CB1 / CB2 / telomere / ACiD) cannot FOUC onto
  New bee if register is late or 404s.

## Graded first paints (Chief PASS, implemented as written)

- **New bee:** calm sentence (“Your body builds signaling from fats it
  cannot make — and this page shows the receipts.”), takeaway lead
  (“The two fats you cannot make write the language.”), quieter support
  (“hypotheses stay labeled — never sold as found.”). Cannabinoid word
  stays out of the first calm breath. Choice: **See the fat story**.
  **Go deeper**. ZERO CB1/CB2 walls, telomere flagship, reversal
  animation, PMID stacks, ACiD unmaking, dose, disease/outcome rows,
  weight mirror.
- After **See the fat story:** one calm story beat — the two essential
  bricks and the blank-honesty line (no official n-6:n-3 target; a page
  printing one invents it), plus the receipted USDA FDC 170148 seed
  numbers. Not the six-stage assembly, not the reverse button, not the
  CB walls on the same beat. Weight mirror = deepen only.
- **Raver:** soft fat/plant wash (seed-oil light → biomass green → teal
  → purple); reduced-motion = one calm frame. Feeling: “What if bliss
  chemistry started in the plate, not the pill?” Tap: **Touch the
  story**. Consciousness (“Cannabinoid receptors did not evolve for
  plants — plants happened to speak the language your fats built.”) is
  layer 2 only, then the New bee takeaway stack.
- **Cypherpunk:** today’s full instrument on contact — walls, six
  stages, reverse hypothesis with grades, mirror inputs, essential vs
  not, evidence ledger, PMIDs, cite-or-silent. Sources use
  `data-view-disclosure` and default open in this view.

Footer keyed leaves (`blong.foot.measure`, `blong.foot.learn`,
`law.hive`) stay laid out so the first-paint floor of 9 does not drop.
`s.blongevity.name` (the word cannabinoid) rides `data-reg="cypherpunk"`
so New bee first paint does not leak it. Cypher masthead clauses ride
`data-reg="cypherpunk"`.

## Language

Thirteen `blong.*` keys were machine-drafted across English plus the 28
docked tongues and marked ⚙. `plate` means dinner plate / food.
`bricks` are the two essential fats. `ratio` is an official n-6:n-3
target that does not exist. `cannabinoid` appears only on the
consciousness / deepen keys. Meaning review is still owed. Existing
`h.088`–`h.095` headings stay on the instrument.

## Verification named here

- `node --test e2e/blongevity-views.test.mjs`
- `node scripts/estate-check.mjs`
- `node e2e/estate-source.mjs`
- `node e2e/university-smoke.mjs` (the existing blongevity instrument
  checks now enter via **Go deeper**)
- browser first-paint screenshots of the three registers at the 8765 poke

Skipped this beat: fleet attestation of the new keys, any health-outcome
rewrite, live USDA / PMID re-fetch (composition and ledger rows are
unchanged), any David Irvine / x0x #622 measurement claim.
