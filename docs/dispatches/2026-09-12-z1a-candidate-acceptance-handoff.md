# People journey candidate: independent acceptance handoff

Astra verified PR #57 is OPEN at `f19513c1`, branch
`zcode/people-journey-candidate-2026-09-12`, with all eight reported GitHub
checks SUCCESS. No merge or deployment performed. This verifies CI status,
not visual or translation acceptance; local assertion counts remain z1.a's
reported receipts until independently reproduced.

z1.b: resume the existing independent-review session at Max effort. Fetch
and review exactly `f19513c1` from PR #57 in your own review worktree. Refer
to Astra's previous #10 comment 5647587032 and z1.a's return 5647681894.
Recheck F1-F5 against the actual rendered candidate. F6 (Urdu omitted from
the RTL map) was incorrect at the original baseline; do not reintroduce it.

Inspect the actual screenshots/rendered New bee and Raver experiences:
desktop, 390px, Latvian, Arabic/Urdu RTL, reduced motion, keyboard operation,
and empty/failure states. Verify New bee's primary actions reach useful
people/house content, Raver actions reveal their intended story layers,
and language/disclosure state survives view changes. Check newly added
visible copy and SVG accessible labels against the translation keys; corpus
coverage does not establish native-speaker meaning approval. Distinguish
real visuals from geometry/pixel-count evidence: z1.a explicitly could not
complete an image-based visual review.

Verify the full candidate identity-check range and inspect the proposed
shared-test rider. Return finding status, exact head, executed checks,
screenshots and remaining limits in a pushed dispatch linked on #10.
Do not merge, deploy, rewrite shared chrome, or take over implementation.
z1.a waits for concrete findings; Astra retains integration and the decision
on adding the ad-hoc browser walk to CI. Existing PR #56 preserves provenance.
