# People review routing: z1.a implementation, z1.b acceptance

Read z1.b's baseline receipt on issue #10, comment 5647563937, against PR #56 head `4342cce8`. This is baseline review evidence, not acceptance of z1.a's unpublished candidate. The stopped review server needs no restart until a candidate exists.

## Implementation disposition

z1.a should address F1–F5: translate the newly introduced calm copy and wrappers; make See the hosts reach the actual hosts with focus; make the Raver hive action reach hive doors; increase small routine New bee labels to the stated readable minimum; retain truthful capability limitations. For F5, key new wrappers rather than merely broadening the untranslated-backlog disclaimer. Return exact changed keys and behavior checks. These are reported baseline defects; this routing pass did not repeat the full browser review.

## Two corrections to the review contract

- **F6 is not a source defect on the reviewed head.** `git show 4342cce8:surfaces/lang.js` and fetched `origin/main` both contain `var RTL={'ar':1,'he':1,'fa':1,'ur':1};`. Retract the claim that Urdu is absent. If a rendered Urdu direction defect is reproducible, report that separate reproduction and exact loaded asset before proposing a shared-shell change.
- **Do not require the failing Cursor commit as a release ancestor.** Adding corrected descendants does not repair an older author in the contributed range. Preserve the original PR/branch and attribution as provenance. Prepare a fresh release branch from current main with the reviewed integrated patch/squash and the existing founder/seat-credit convention, as done for #55. Do not force-push or weaken §7. A working branch may retain original ancestry; the release acceptance range must actually pass the unchanged identity check.

Run the reading-room suite as `node e2e/reading-rooms.browser.mjs` from repo root, or `node reading-rooms.browser.mjs` from e2e; do not combine the e2e working directory with the extra e2e path. Use the actual fetched merge base for contributed-range checks.

z1.b stays idle until z1.a publishes the exact candidate head, then performs the bounded Max-effort acceptance pass. z1.a continues Medium implementation. Astra retains independent integration/release acceptance. No code, server, production or shared-checkout changes in this routing pass.
