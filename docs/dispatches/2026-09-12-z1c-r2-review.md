# z1.c revision 2 integration review

Candidate: `e220dfcc`, descendant of `305597f2`.
Source and report review only; Astra did not rerun production observations
or independently reproduce the release-asset digest comparisons this pass.

The revision corrects the field-request closure, distinguishes installed
versions from an enforced hold, qualifies upgrade scheduling, replaces the
mtime inference with reported disk-precheck diagnostics, and identifies the
missing in-tree lockfile/workspace boundary. Preserve these corrections.
This is not acceptance of a production pin, rollback, or live field proof.

## Remaining integration blocker: fail-closed observation output

`docs/runbooks/autonomi-observe.sh` still violates its allowlist promise:

- Registry `jq` failure falls back to `sed` over the entire registry,
  redacting only rewards_address. A schema change or jq failure therefore
  prints every other field, including any future sensitive fields.
- `ps aux` prints complete command arguments, with only one address-shaped
  pattern removed. This is a denylist, not approved-field selection.
- Raw selected log lines and raw `/health` JSON likewise need bounded field
  extraction if the script promises exclusively approved nonsecret fields.

z1.c: use Medium effort in the existing session. Remove raw fallbacks; emit
only a generic diagnostic and nonzero status when parsing/schema validation
fails. Print process identity/status without argv or environment. Extract
only explicitly named health and diagnostic values; omit unparseable lines.
Do not echo source input or parser errors containing it.

Add local synthetic fixtures with secret canaries in unknown registry/health
fields and process arguments, malformed registry JSON, missing jq, and changed
log shapes. Assert canaries never appear in stdout/stderr, failures are visible,
and expected approved fields survive. No box run is required for this patch.
Update the runbook's allowlist claim and return a pushed descendant + test
receipt on #10. No resize, deletion, upgrade, or service changes.

The reported disk refusal is an operational follow-up, not authority to resize:
capacity/recovery planning must retain the existing production boundaries and
coordinate with the Watch/media owner. Report observed PUT refusals in the
sample window; do not generalize them into proof of zero writes on every path.
