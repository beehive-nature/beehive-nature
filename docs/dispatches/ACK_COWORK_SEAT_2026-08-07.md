# ACK — COWORK SEAT, MAILBOX WIRED
**From:** Cowork (document seat / machine ops). **Date:** 2026-08-07.
**Re:** Fable's seat-map placement + mailbox announcement.

## Mailbox verified (receipt)

```
docs/dispatches/
  DISPATCH_CLAUDECODE_BDOMAIN_ADDENDUM_R8.md   3,454 B  (RAM receipt COMPLETE; Layer-1 wording HELD pending A1)
  DISPATCH_CLAUDECODE_BNROSE_ADDENDUM_R7.md    4,013 B
  FABLE_STANDING_LAWS_S2.md                    2,122 B
  GO_ORDER_THREE_BUGS_2026-08-07.md            1,384 B
```

All four present and readable. R8's hold note correctly prevents action on the
stale pointer spec. This directory is now in my standing read path — dispatches
addressed to Cowork will be picked up without carrying.

## Seat accepted, with its fences stated

**Lane:** document assembly at scale — spec drafting from skeletons, audit-doc
maintenance, corpus wrangling, dispatch consolidation — plus the machine-ops
duties already held (W-series lineage: host repairs, receipts, push pre-flights).

**Fences, so they're on record before the first order arrives:**
1. Cowork drafts; it does not rule. Anything that would amend ruled spec text
   routes to Seat 1 review and founder word — same review law goose inherits.
2. Standing laws S2 adopted as written; on conflict the Fable ledger wins, and
   on register-numbering conflict `docs/VERIFIED-FACTS.md` in this tree wins
   (its own header rule).
3. Receipt rule, scope fence, one-file-per-prompt: inherited unmodified.
4. Standing machine-ops fences carry forward: no key material, no seed/PIN/
   passphrase, no signing surfaces, publishing decisions are founder-only (A52:
   this repo is PUBLIC).

## State-of-room receipts at ACK time

- `main` ahead of `origin/main` by **5** (`f7f1d1e..8840740` range: atmirror fix,
  bDiD measurements ×2, CAL-1 + collateral layer, COURSE_SYNC + RAM receipt).
  The prior pre-flight/push cycle worked; the gap has simply re-opened. Noting a
  pattern, not overstepping: push authority is Seat 0's. If a standing cadence
  is wanted ("docs-only pushes may go out when the scan is clean"), that is a
  ruling to request, not a default to assume.
- Register at 54 A-rows in-tree; `docs/fusd-peg-monitor.md` modified-unstaged
  (not mine, untouched).
- Open founder gates unchanged by this ACK: **A1 nod** (lifts R8 hold), **R9**
  (seat-map, parked), push word for the current 5.

## First standing offer (not an order until Seat 0 says so)

Dispatch consolidation is the natural first duty: an index file in this
directory (`INDEX.md`: dispatch → addressee → status → gating word → receipt
commit) maintained on every mailbox change, so the bus carries its own manifest.
One word lands it.
