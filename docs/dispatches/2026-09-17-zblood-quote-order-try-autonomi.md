# ORDER — zBlood storage-economics lane: QUOTE ONLY for the Bux try_autonomi.mp4 media object (no upload, no spend)

FROM: founder review of the W@tch intake lane, 2026-09-17
(zCode seat docketing; receipt at `docs/dispatches/2026-09-17-watch-try-autonomi-intake.md`,
branch `zcode/watch-try-autonomi-2026-09-17` @6bca37e8)

You own the storage-economics lane (measured-states law). The W@tch intake lane produced
a real, provenance-pinned 204 MiB media object whose playable W@tch future runs through
your machinery. Founder order: obtain a **LIVE ANT STORAGE QUOTE — QUOTE ONLY**.

## Exact object identity (the quote must bind to THESE values)

- Original filename: `try_autonomi.mp4`
- Exact size: **214,091,829 bytes** (204 MiB — quote the real byte count, not "204 MB")
- SHA-256: `338b486874f6a8f86afe6537143548fb99594038b9ee7de918794db9e744207e` <!-- PUBLIC-CONSTANT: sha256 content pin, community video intake -->
- Media identity: "Autonomi — WebRTC Direct in-browser demo", creator "Bux · Autonomi
  community update, 2026-09-17" (full intake card in the receipt §3)
- Container/codecs: MP4 (isom), H.264 Main@L5.1 3318×2132 ~60 fps, AAC-LC 48 kHz mono,
  52.488 s
- Artifact location: the founder's laptop, `C:\Users\travi\Downloads\try_autonomi.mp4`
  (untouched, byte-exact — your ceremony reads it; never moves, never re-encodes)

## Scope law for this order

- **QUOTE ONLY.** No purchase, no PaymentIntent/PricingCommitment execution, no upload,
  no state mutation on any funded identity. If your ceremony's UI exposes a pay button,
  the run stops before it.
- Measured-states law binds (quote ≠ purchased ≠ uploaded ≠ retrieved ≠ hash-verified):
  the quote must be LIVE from the network, timestamped, ANT-denominated, with the basis
  recorded (record-count state per the pricing function — quadratic in node RECORD
  count, per-byte REJECTED). Nothing synthetic may be labeled measured.
- A real 204 MiB media quote is the point: this replaces synthetic $/GB arithmetic with
  a community-relevant object. Report the quote with its timestamp and network state so
  a later price drift is honestly comparable.

## Where this quote slots in the chain (context, not your scope)

source media → provenance/hash ✓ → W@tch intake (draft staged) → **Autonomi quote ← THIS
ORDER** → PricingCommitment / invoice → bounded authorization → ANT upload →
DataMap/address → W@tch playable object (intake draft consumed, credits re-keyed onto
the address) → independent retrieval → playback verification.

Each later gate is a separate founder-gated ceremony with its own receipt; this order
opens exactly one of them.

## Expect receipts (AGENTS.md law)

Reply with: quote value (ANT + timestamp + network/record-count basis), ceremony build
used, and an explicit BOUNDARY NOT CROSSED line (no purchase, no upload). Bank into
docs/dispatches/ and push per the reports-in-tree law.
