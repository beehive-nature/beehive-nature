# Bloom still — original packed JPEG

`green-teal-bloom.jpg` is the JPEG LoVis and his mother supplied, extracted
from the packed `<image>` inside
`docs/mvp-walk/assets/genesis-3d/motion/green-teal-breathing.svg`.

It is a resting still of the original artwork. It is not a Blender relief,
not a mint, and not a licensed media copy. Motion study (breathing SVG)
remains Astra’s; do not rewrite the Blender builders on draft #31.

This still exists so a share card and first paint can stay lightweight
without fetching the cell-path SVG.
