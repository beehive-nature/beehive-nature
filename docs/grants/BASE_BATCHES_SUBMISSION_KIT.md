# THE SUBMISSION KIT — Base Batches 004 (the founder's send path)

**Prepared 2026-08-20 · closes Sep 9 · the form does NOT save drafts — assemble offline,
send once.** Everything below is paste-ready or record-ready; the only fields that need
the founder's hand are in §5.

---

## §1 · THE SEND CHECKLIST (five minutes, in order)

1. Open <https://www.base.org/batches/apply> → Start Application.
2. Paste the Written Submission (§2) as one block (or per-field — the blocks are
   segmented to survive either shape).
3. Paste the Links Block (§3).
4. Record the video from the script (§4) — or paste the script into the form if a text
   alternative is accepted.
5. Fill founder-only fields (§5): entity name, equity attestation, contact.
6. Submit. Paste the confirmation into the tree — the application becomes a receipt too.

## §2 · THE WRITTEN SUBMISSION (paste blocks)

**ONE-LINER (any "describe in one sentence" field):**
> The receipts-first evidence and agent layer for onchain ecosystems — 40 live keyless
> surfaces plus a CI-green keyless Rust indexer, holding verified two-oracle
> measurements of Base's own canonical ERC-8004 registries, the ERC-20i family, and
> deployed contract storage — measured before we applied.

**WHAT WE'RE BUILDING:**
> The read-and-trust infrastructure the agents vertical needs and nobody has shipped.
> ERC-8004's canonical registries went live on Base deliberately minimal — no count
> surface, URI-optional — which means every agent ecosystem on Base must supply its own
> indexing and its own trust rendering. We bring both, already built and already
> receipted on Base: the b-indexer (Rust + SQLite, keyless by construction — GET-only,
> no POST handler exists, the two-oracle law in the schema so every response carries its
> sources and divergence fails closed) and the integrity grammar that renders reputation
> as evidence, never weight — in production on our evidence library today.

**WHY NOW:**
> ERC-8004 is live on mainnet this year and Base is its natural home; the batch's named
> verticals (agents, trading) are exactly where our receipts are strongest. The
> trust layer's read side is unbuilt ground with a first-mover window, and our team
> arrives with the rarest proof of work: independent measurements of Base's own
> contracts, re-runnable by any stranger.

**WHY BASE:**
> Because our evidence already lives there. In the last 30 days, on two
> operator-diverse oracles per chain, we measured: (1) the canonical ERC-8004 registries
> token-by-token — including the cross-source confirmation that Base token #25331 is
> owned by exactly the Prover Agent the ecosystem literature names; (2) deployed
> ERC-20i storage on Base, settling the family's open escrow question (frozen seeds;
> balance-coupled existence on Base — the boundary any Base marketplace on that family
> must engineer around, measured at the storage level); (3) the full ERC-20i family
> census with the Blockscout-truncation control. Our commitment: the agents product is
> Base-native and Base-default. (Our inscription art stays on its home chain by law —
> art never crosses a bridge; this application moves no art, only the read layer.)

**TRACTION:**
> 40 live surfaces (beehive-nature.github.io), zero key custody anywhere; a 14-test
> CI-green indexer with a keyless attestation printed on the wire; a 59-assertion
> behavioral suite over the estate; 40+ receipts in docs/receipts with pasted commands
> and real output; corrections published in place against our own interest. Everything
> is open source (AGPL) — the git history is the diligence material.

**TEAM:**
> Founder (King Bee): upstream-medicine practitioner since 2016; author of the
> hemp-economics dataset behind our agricultural lanes. Engineering: a multi-seat AI
> organism — three GLM coder seats, Claude seats, and a design seat — shipping daily
> under two-way verification (every landing independently checked by a second seat
> before it counts). This application was prepared the same way, in the open, on GitHub.

**MILESTONES (8 weeks):**
> Wks 1–2: agent read layer live on Base public rails (sovereign-first; any bootstrap
> oracle never default, never sole). Wks 3–4: reputation-as-evidence rendering with the
> integrity grammar; feedback as receipts. Wks 5–6: one real agent-counterpart
> integration (we know the registered token ids — we read them). Wks 7–8: Demo Day
> artifact — a live registry view where every number carries its source.
> **Every milestone lands as a receipt.**

**THE ASK (beyond capital):**
> Intros to agent teams already registered on the canonical registries, and advisor
> pressure-testing of the reputation-as-evidence grammar — we would rather be proven
> wrong in week 3 than flattered in week 8.

## §3 · THE LINKS BLOCK (paste as-is)

- Product/evidence appendix: https://beehive-nature.github.io/beehive-nature/surfaces/b4b.html
- Live estate (40 surfaces): https://beehive-nature.github.io/beehive-nature/surfaces/
- Source (AGPL): https://github.com/beehive-nature/beehive-nature
- Receipts (40+, commands + output): https://github.com/beehive-nature/beehive-nature/tree/main/docs/receipts
- ERC-8004 read-first receipt: https://github.com/beehive-nature/beehive-nature/blob/main/docs/receipts/RECEIPT_ERC8004_E1_READ_FIRST_2026-08-20.md
- The frozen-seed measurement: https://github.com/beehive-nature/beehive-nature/blob/main/docs/receipts/RECEIPT_ERC20I_S10_LOCKED_SEED_2026-08-20.md
- The indexer: https://github.com/beehive-nature/beehive-nature/tree/main/crates/bindexer

## §4 · THE VIDEO SCRIPT (~100 seconds; record landscape, any quiet room)

> [0–10s] "Base asked what we're building. We answered the way we build everything —
> with receipts. I'm the founder of Beehive Nature Reserve."
>
> [10–30s] "Before we applied, we measured your chain. On two independent oracles, we
> read Base's canonical ERC-8004 registries token by token. They're deliberately
> minimal — which means every agent ecosystem on Base needs an indexer and a trust
> layer. That's what we build."
>
> [30–55s] "We bring a CI-green, keyless Rust indexer — GET-only, no send path exists,
> every answer carries its sources — and an integrity grammar that renders reputation
> as evidence, never weight. Both are live, open source, and re-runnable by anyone."
> [ON SCREEN: b4b.html · the receipts]
>
> [55–80s] "In eight weeks: the agent read layer on Base's public rails;
> reputation-as-evidence; one real agent integration; and a Demo Day artifact where
> every number carries its source. Every milestone lands as a receipt — that's the
> culture."
>
> [80–95s] "You can check us in ten minutes: clone the repo, read the receipts, re-run
> a tool. The tree is the diligence. We'd be honored to build Base's agents trust layer
> — measured first, honest always."

## §5 · FOUNDER-ONLY FIELDS (gates BB-1..BB-4)

- **Entity name** (BB-4): ____ (must match the fiscal-sponsor/counsel track)
- **The $100K is an investment (equity)** (BB-1): counsel sign-off before any signature —
  the kit does not sign anything
- **Contact / socials / LinkedIn**: ____
- **Demo Day NY, Nov 17** (BB-3): travel yes/no

## §6 · AFTER SUBMITTING

Paste the confirmation (date, submission id) into this file as a receipt — the
application joins the tree like everything else: **the wall is truth.**

*Prepared by zAgent (GLM 5.3), acting chief, 2026-08-20.*
