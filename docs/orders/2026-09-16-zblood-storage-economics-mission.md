# MISSION ORDER — zBlood / `lane/zcode-lineage-import` seat

**From:** founder, relayed by the zCode surface-polish seat 2026-09-16.
**To:** the existing zBlood workerb (it owns the snapshot, preservation manifest, restore verification, person staging, and ANT/AR handoff — this experiment belongs there, not in SRE, zArcheology, or the general Autonomi research lane).

---

## zBlood Mission: FIRST REAL STORAGE-ECONOMICS RECEIPT

Use the **first approved zBlood public preservation snapshot** as the experiment — not a synthetic 1 GB/TB example.

### A. Measure the artifact
- exact manifest SHA-256
- exact bytes/files/person objects/evidence packs
- compression/package form actually uploaded
- timestamp

### B. Quote Autonomi at upload time
- query the live production pricing route immediately before purchase
- preserve raw quote response, ANT/USD reference used, network composition (Merkle/quotes if exposed), timestamp and expiry
- compute actual `$ / GiB`
- quote ≠ purchase: label it explicitly

### C. Purchase/upload only after the existing approval gate
- package must first pass the preservation adversarial fixes
- founder approval binds exact manifest + destination
- upload to ANT
- bank transaction/payment/storage receipts and resulting content addresses
- never publish protected/living-family material

### D. Prove retrieval independently
- fresh destination/root, not the source worktree or upload cache
- retrieve from ANT
- verify every artifact against the approved manifest
- restore and run the existing acceptance journey:
  `profile → crest → Albert Perry Rockwood/Donna → person archive → blood fractal → evidence/lore`
- only then mark **ANT retrieval-verified**

### E. Economics experiment
Model the *same immutable snapshot* at:

`1 · 10 · 100 · 1,000 · 1,000,000 retrievals`

Record separately:
- initial ANT upload expenditure
- Beehive Nature storage renewal expenditure
- Beehive Nature retrieval/egress expenditure
- any infrastructure we actually operate that does scale with retrievals
- comparable S3/CloudFront, B2, R2, etc. assumptions with retrieval/egress explicitly separated from storage

The headline is **not predetermined**. Test:

**"Does Beehive Nature's marginal storage/egress obligation for this already-purchased immutable archive remain effectively unchanged as independent retrieval count rises?"**

If reality disagrees, report reality.

### F. Make it reproducible
Create `zblood-storage-economics.json` + human-readable receipt with formulas and raw inputs. No hard-coded `$747/TB` claim. Every price is timestamped evidence.

### G. AR comparison
Quote the identical approved manifest/package for Arweave separately. Do not automatically duplicate the whole archive. First measure whether AR is better used for the compact permanent provenance/root manifest while ANT carries the archive payload.

**No synthetic projection may be labeled measured. Quote / purchased / uploaded / retrieved / hash-verified are five different states.**

---

## Public demonstration path

**Albert Perry Rockwood as the public demonstration path** for this receipt — the case study becomes understandable to a stranger: open a real ancestor → inspect biography/records/lore → traverse the fractal → retrieve the underlying archived evidence.

Then a small live card into `blood.html`:

> **⬡ This family archive cost ___ ANT / $___ to preserve on ___**
> **retrievals tested: ___ · developer egress bill: ___**

That turns the Discord argument into a **running empirical experiment attached to an actual decentralized application**.

## THE GATE (founder, verbatim)

**Do not purchase/upload until the preservation adversarial fixes from the last review are green.** Quote collection can start immediately; money/storage publication follows the exact-manifest approval gate.
