# REGISTER — preapproved adapters/chains (gate R-5, seeded by founder word 2026-08-20)

**Standing rule:** an adapter/chain enters this register by founder word only — never by
default, never by a seat's convenience (resurrection doctrine, DISPATCH_SOULCATS §6.2;
adapter-ring invariant, SPEC-PAY-ONCE-NOW-1 #3). Every durable record is self-describing
and additive-only, so a new adapter never re-mints anything.

Founder's seeding words, verbatim: *"use pages and repos and as our adapters [eternal
immutable data networks and vaulta/hive {BCH back up anchor with exSAT]"*

| adapter | role | in-tree status (2026-08-20) |
|---|---|---|
| **Vaulta** | identity anchor: `.b` registry (`kingbeelovis`), BEELOG epoch roots, bzDiD Layer-2 | LIVE — registry on mainnet; `chain-eos` decode + `wallet-relay` unsigned-tx prep in-tree |
| **Arweave** | eternal immutable store (pay-once): epoch DataItems, receipts, capsules | LIVE — `atmirror::arweave` builds real ANS-104 DataItems, per-name claimant-paid per the ruled invariant |
| **Autonomi** | eternal store, second ring member; Tier-2 persistence (encrypted append-only logs) | LIVE rail (`ant` driver) — COLLISION-1 (2.0 immutability vs mutable-pointer shape) still open |
| **Hive** | content-is-chain longform + free-at-point-of-use Resource Credits; founder holds the account | RESEARCHED, parked in the candidates ledger (markdown→Hive + media→eternal ring + receipt→anchor hypothesis; ~64KB post cap known) — **promoted to the register by this seeding; build still gates on its own lane opening** |
| **exSat** | EVM lane (chain 7200): inscriptions, PermaLock discipline, the bRoSe OFFER target | LIVE lane — bnri-contracts built to S-7; deploy rehearsal owed founder-present |
| **BCH** | **BACKUP ANCHOR** (founder word) — a second, independent anchor lineage | NEW by this seeding — the workbench already carries BTC/BCH readers; the anchoring mechanism (OP_RETURN-class commitment cadence, who pays, what anchors) is **research + options owed** before any write |
| **GitHub repos + Pages** | the mirror/serving layer — "the tree is the relay of record"; surfaces served, docs mirrored | LIVE — beehive-nature, LOVErnment-DAO, b-domain, bnri-contracts, bnr-design-system all public; NOT a system of record for anything durable (PAY-ONCE #4: local-first, eternal ring for durables) |

**Fences that ride along:** Article V.1 (users fund what they consume — an adapter never
makes the hive a payer); the hardline 8d test (does the ten-billionth user's cost land on
BNR?); rule 10 fail-closed at every resolution seam; a page must not be its own witness
(two independent sources for any chain fact).

**Exit/change:** an adapter leaves or changes role by founder word, recorded here with
the receipt. Seat 3 maintains.
