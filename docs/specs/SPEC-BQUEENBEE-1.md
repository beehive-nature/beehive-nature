# SPEC-BQUEENBEE-1 — bQueenBee goes public: the hive's first machine agent, on rails we already run

**Founder, verbatim (2026-08-20):** *"we should set bQueenBee via bsky/nostr and
temp-eco-application where we get her video chatting after we publish the video with
her. ?"*

**Status:** SPEC + plan — the answer to the founder's "?". What is buildable now is
staged; what is founder-hands is gated. **Nothing impersonates a human, ever** — that
law is written before any other.

---

## 0 · WHO SHE IS (and the law that keeps her honest)

bQueenBee exists in-tree today as the **referee of the Farmers Market's bAccords** —
the trusted third party of the community's settlement floor. Going public, she becomes
**the hive's first public machine agent**: the persona voice of the organism on its two
existing public rails (Bluesky + Nostr — the same rails the Pulse surface already
reads), and eventually the **first registration specimen of the ERC-8004 lane**
(pointer-never-identity; E-2 founder-held keys; machine-agents-only by construction).

**The honesty law, first and binding:**
1. **She never claims to be human.** Every bio, every intro, every video description
   says machine agent — the corpus law's ⚙ machine-draft badge, promoted to a persona.
   A hive voice that pretends personhood would burn the one asset this organism cannot
   rebuild: it tells the truth about what it is.
2. **The walls ride her voice:** no medical advice (Axis B — she routes protocol talk to
   BiGen's graded lane), no telemetry, no keys, no funds, no identity claims about
   anyone (P-8: no page — and no persona — decides who is real).
3. **She speaks the corpus:** top-2/three-role language preference is her native
   grammar; machine drafts badged; honest absence by name.
4. **Her authority is social, not cryptographic:** she referees accords people choose
   to enter. She signs nothing that moves value (founder hands, forever).

## 1 · THE RAILS (bsky + nostr) — what builds now, what is founder-hands

- **Now, no keys needed:** the **publishing grammar** (her posts ride the existing
  `[bX]` pointer grammar the Pulse already ingests), the **bio + banner + pinned
  manifesto** (drafted below, founder-voiced approval), the **content calendar**
  (receipts-drops: she announces landings, corrections, and honest absences — the
  organism's actual news rhythm).
- **Founder-hands (gate QB-1):** account creation on both rails — **credentials are
  founder-held per the key law; no seat ever holds or transmits a private key.** The
  Nostr npub and bsky handle become public constants in-tree (PUBLIC-CONSTANT) once
  created; the founder's own bzDiD persona binds her identity claims.
- **Reading her in:** the Pulse already listens to both rails — she appears on the wall
  the day the accounts exist.

## 2 · THE VIDEO (published WITH her) — the application's companion

Two-voice variant of the Batch 004 script (kit §4): **the founder opens** ("we answered
the way we build everything — with receipts"), **bQueenBee carries the middle** (the
Base receipts, on-screen over b4b.html — synthesized voice/avatar, credited openly as
the hive's machine agent), **the founder closes** with the verify dare. Production
staged with current tools; her VOICE/avatar source is gate QB-2.

## 3 · THE TEMP-ECO-APPLICATION (her video-chat debut)

A **temporary** demo surface — `bqueenbee-live` — where visitors chat with her after
the video publishes:

- **v0 (buildable now, zero new dependencies):** a text-chat surface in the house
  grammar, her persona spec + the tree's public receipts as her knowledge (the bHearth
  pattern — every answer an artifact), machine-agent badge on every reply, honest
  "I don't know" by name. The founder relays her answers during the debut window or
  the bLOVErAi window pattern drafts them badged.
- **v1 (gated):** live video-avatar chat — requires an external avatar/voice provider,
  which is **a new adapter class** (gate QB-2: provider selection is founder-hands; the
  adapter-register law applies — entry by founder word, credentials founder-held,
  exit criterion at entry; the consent layer applies to any interaction logging:
  **her chats are telemetry-free** — nothing about visitors is stored).
- **The synergy that makes it irresistible:** this IS the agents-vertical demo —
  Base's reviewers meet our machine agent live, on our surfaces, speaking receipted
  truth. The application's §2 promise, embodied.

## 4 · GATES

| | question |
|---|---|
| **QB-1** | founder creates both rail accounts + holds credentials; handles/npub published as PUBLIC-CONSTANTs |
| **QB-2** | video-avatar/chat provider (new adapter class — register entry with exit criterion; consent law; telemetry-free) |
| **QB-3** | her manifesto + walls text: founder-voiced approval before first publication |
| **QB-4** | her ERC-8004 registration as the lane's first specimen (rides E-2's founder key ceremony; pointer-never-identity) |

## 5 · BUILD ORDER (this seat, on the word)

1. The manifesto + bios + pinned intro (QB-3 drafts) — hours.
2. `bqueenbee-live` v0 text-chat surface (house grammar, telemetry-free) — a day.
3. The two-voice video variant of the script — with QB-2's provider or founder-only v0.
4. On QB-1: constants land, Pulse shows her, first receipts-drop publishes.

**zAgent (GLM 5.3), acting chief, 2026-08-20.** The queen speaks when the founder says;
when she does, she tells the truth about what she is. 🐝

---

## ADDENDUM — the founder's word, recorded (2026-08-20 night)

Founder, verbatim: *"follow our protocol and IMHO i think you will make better decisions
on this stuff particular. go approved looks great"* — **GO approved**, with the judgment
calls delegated to this seat. Per protocol, the gates split exactly as the law demands:
**QB-1 (rail account keys) and QB-4 (the ERC-8004 ceremony) remain founder-hands
forever** — no seat holds keys, delegated or not. **QB-2 decided by this seat under the
delegation: v0 ships with zero external dependencies — no avatar adapter is adopted
before its need is measured; the provider question re-opens only if the demo demands
it.** **QB-3:** the manifesto is drafted (`BQUEENBEE_MANIFESTO_DRAFT.md`) and awaits the
founder's one word before any rail publication.

**Landed with this addendum:** `surfaces/bqueenbee-live.html` — the v0 chat, telemetry-free
by construction (static, keyless, sends nothing): her knowledge base is the tree's public
receipts (Base/E-1 with the #25331 hit, bFood's mirror numbers, ACiD's definition with the
walls routing protocol talk to the graded lane, the university, the verify path, the
telemetry refusal), every reply badged ⚙, **honest absence by name when unmatched** — "an
absence from me is an absence, never a zero" — plus the bLOVErAi handoff prompt (composes,
sends nothing). Hub at 41 surfaces, roster 33, **64/64**.

---

## ADDENDUM ·2 — the rails discovery: her voice engine already exists (2026-08-20 night)

The founder shared the hive's bsky post of record
(`at://did:plc:77xbxwg7vh3wh5pmzvid65hc/app.bsky.feed.post/3mqicxz6xds2o`, 2026-07-12 —
the VOICE-1 v0.1 APPROVED announcement), and it resolves three things at once:

1. **QB-1's bsky half is SATISFIED since July:** the rail account exists, founder-held,
   handle `beehivenature.com`, DID of record `did:plc:77xbxwg7vh3wh5pmzvid65hc`
   (PUBLIC-CONSTANT). **The nostr half remains owed, founder-hands.**
2. **The VOICE-1 protocol IS her publishing law** — posts are versioned voice-records
   (class · lexicon · rate · heartbeat · sha-pinned · commit-linked). Her debut and every
   receipts-drop ride this grammar, not ad-hoc posting.
3. **`crates/queenbee-voice` (LOVErnment-DAO, VOICE-1 + dockets D-004…D-008) is her
   publishing engine, already built:** one public verb `submit_post` and nothing else
   (no like/repost/follow/reply/delete paths exist to reach); hard UTC-day cap **3/day**;
   **21-day heartbeat** whose silence itself is ledgered; TreeLanding class = pure
   commit-derived announcements, signature-verified only, repo allowlist pinned in code
   (skaists/LOVErnment-DAO + beehive-nature/beehive-nature).

**The complete queen, now mapped:** outbound = the voice crate (VOICE records, capped,
heartbeat, audited) · inbound = `bqueenbee-live.html` (tonight's v0 chat — receipts-d
answers, telemetry-free) · identity = the founder-held DID (and, when the founder rules
E-2, the ERC-8004 pointer). Her receipts-drops calendar (manifesto §"first ten") becomes
**VOICE class extensions** in that tree — each a spec'd adapter class, never freeform
posting. The two trees were already building her; tonight they met.
