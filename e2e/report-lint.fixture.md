# report-lint fixture — four units, four intended classifications

The estate review ran tonight and the gates pass 8/8.

```
PASS R1 every counted surface is reachable
PASS R4 arithmetic reconciles
8 passed, 0 failed
```

The merge landed clean and CI is green everywhere.

The CDN still serves the pinned 4.5.1 build and matches the vendor record (UNVERIFIED: not re-fetched at report time).

The reviewer accepted the lane and the count matched the tree.

This sentence is background prose about the fixture itself and asserts nothing.
