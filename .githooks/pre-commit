#!/bin/sh
# Pre-commit: delegate to the shared secret scan (rules, exemptions, and the
# TESTNET-ONLY vector path are documented in scripts/secret-scan.sh).
# CI runs the same script in `tree` mode on every push as the remote backstop.
exec sh scripts/secret-scan.sh diff
