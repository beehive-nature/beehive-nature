[toolchain]
# Pinned to what CI runs (founder order 2026-09-03): stops "agreed by luck"
# drift; the box updates itself via rustup on next cargo call.
channel = "1.98.1"
components = ["rustfmt", "clippy"]
