# BUG-002 — design battery D4/D5/M gaps on bmeshasi.html

## S.O.A.P NOTE (at presentation)

- **S — Subjective:** the bmeshasi surface (pledge your iron, metered, paid) has no hero figure, no 600px media query, and D5 flags density/radius/padding gaps. Found during the stranger walk and recorded as pre-existing.
- **O — Objective:** same structural class as BUG-001 — the page was built as a data surface (dials, tables) without the design battery's structural requirements.
- **A — Assessment:** pre-existing, not a regression. The page's live chain reads are honest (dials never invent) — the gaps are purely presentational.
- **P — Plan:** scheduled for a surface bring-up lane (same class as BUG-001's fix: add section wrapper, hero figure with a live dial count, 600px media query, D1 inset via a .pstat element, D2 sem tokens).

## OPERATIVE REPORT (at closure)

- **Status:** CLOSED — design battery 14/14
- **Preop dx:** data surface without battery structure — D1 no inset step, D2 no --sem-* tokens + no meanings comment, D3 headline not gradient-clipped, D4 no hero marked, M no ≤600px media query, I1 cross-origin chain reads at page-open (battery 7 passed / 7 failed @ main 0e1c22ec)
- **Postop dx:** data surface WITH battery structure — sem tokens, gradient headline, .pstat inset hero, 600px query, instant-law page-open — meter math, failover walk, waterfall and gate copy untouched
- **Procedure:** structural alignment only — --sem-* tokens aliased to the page's own palette with the meanings-first comment; h1 gradient-clipped; hero figure bound to the live Vaulta head-block dial (honest empty state, amber dead-read, caption narrates state); ≤600px media query; first chain read deferred to a beat after first paint (15s cadence / failover / visibility-pause / amber laws unchanged)
- **Findings:** the page's meter semantics and live-read lanes were already correct; the battery structure was additive. Stale "three-host failover" meta corrected to two — the dead-host sweep's actual count.
- **Complications:** the cheap I1 fix would have been adding the chain hosts to the gate's RIDER_ALLOWLIST — every entry there rides a founder order, so that file is out of lane bounds; the in-lane honest fix is the deferred first read (page-open issues zero chain requests; the dials hydrate after first paint and never invent).
- **Closure receipt:** design-acceptance 14/14 on surfaces/bmeshasi.html · estate-check PASS · university-smoke 87/87 · no-page-errors 104 surfaces / 0 errors · no-dead-host + atlas + agent-dock 49/49 · lane bkimi/bug-002-bmeshasi-design · dispatch docs/dispatches/2026-09-16-bug002-bmeshasi-design-battery.md
- **Operating seat:** bKiMi
- **Date:** 2026-09-16
