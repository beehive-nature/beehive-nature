# BUG-001 — design battery D4/D5/M gaps on qrroses.html

## S.O.A.P NOTE (at presentation)

- **S — Subjective:** the qrroses page has no hero number, no 600px media query, and the density pass flags padding/radius/border-radius as absent. Reported by the design-acceptance battery.
- **O — Objective:** battery output: D4 "no hero marked", D5 "type=13px radius=0px padding=26,20", M "≤600px media query exists" = false. The page is a pure canvas art surface with no section element and no responsive breakpoint.
- **A — Assessment (differential):** pre-existing structural gaps on a canvas-first art page that was never design-gated. Not a regression from any landing — the page predates the battery. Fix class: structural alignment (add section wrapper, hero figure, media query) — same pattern as market.html and museum.html fixes.
- **P — Plan:** fixed in the QR ROSES lane (2026-08-30) — canvas wrap became `<section class="wrap">`, hero = 7 stems (the live odd-count), 600px query added, D1 inset via `.well` tone, D2 sem tokens declared. Battery went to 14/14.

## OPERATIVE REPORT (at closure)

- **Preop dx:** art canvas surface, no battery structure
- **Postop dx:** art canvas surface WITH battery structure (section, hero, inset, media query) — art unchanged
- **Procedure:** structural alignment only — no art code modified
- **Findings:** the page's own canvas/rose generator was already correct; the battery structure was additive
- **Complications:** the odd-law gate initially caught the author's own four-layer rose (even layer count) — the gate gates the author
- **Closure receipt:** design-acceptance 14/14 on surfaces/blight/qrroses.html @9f3e4aa
- **Operating seat:** z2.1
- **Date:** 2026-08-30
