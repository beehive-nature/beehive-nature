[package]
name = "spike"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "bSAFE 7 host spike: THP-over-UDP against trezor-user-env; one signed message round-trip from an htmx surface (goal state, see SPIKE.md). v0: datagram transport leg; bsafe-thp wired for the noise layer."

[dependencies]
bsafe-thp = { path = "../thp" }

[[bin]]
name = "spike"
path = "src/main.rs"
