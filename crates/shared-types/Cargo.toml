[package]
name = "shared-types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "bNature canonical event schema — the lingua franca of the kernel (brief §9.3)"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
