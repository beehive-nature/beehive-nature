[package]
name = "bmesh-hwfit"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Hardware-fit auto-scoring for bMeshLLM node scheduling (SPEC_DOCTRINE-HARVEST-1 D7)"

[dependencies]
serde = { version = "1", features = ["derive"] }
sysinfo = "0.30"
